# 600125_Module.md

Status: Implemented
Lifecycle: Module
Stage: 4
Owner: Codex
Date: 2026-07-11

## Summary

Implemented the approved `600120_guest_customer_bootstrap_rpc` change and recorded the Human-approved 짠24 follow-up fixes required to make the already-applied live RPCs actually run end-to-end in the local Docker database.

Primary forward migrations:

| File | Purpose | Result |
|---|---|---|
| `sql/migrations/0149_create_guest_customer_bootstrap_rpc.sql` | Create `catchmenu_store.get_or_create_guest_customer(...)`; patch `catchmenu_pos.register_waiting(...)` and `catchmenu_store.bootstrap_customer_app_v2(...)` so omitted `p_customer_id` is filled by a guest customer. | Created, applied, directly re-executed where required. |
| `sql/migrations/0150_widen_event_domain_constraint.sql` | Widen `catchmenu_ledger.events.chk_event_domain` to allow `waiting`, because `register_waiting()` writes `event_domain = 'waiting'`. | Created and applied through `tools/apply_migrations.py`. |

## 짠24 Follow-up Fix Summary

These were not redesigns. They were Human-approved lightweight corrections for pre-existing stale column references discovered during the first complete local execution of the 600120 flow.

| File | Correction | Reason |
|---|---|---|
| `sql/migrations/0081_create_customer_app_rpc.sql` | `customers.total_points` ??`point_balance`; `customers.total_spend_amount` ??`lifetime_spend`. | Actual `catchmenu_store.customers` columns are `point_balance` and `lifetime_spend`. |
| `sql/migrations/0097_create_auth_login_pipeline_rpc.sql` | `customers.total_points` ??`point_balance`; `customers.locale` ??`preferred_locale`. | Actual customer profile columns differ from stale RPC references. |
| `sql/migrations/0108_create_membership_pipeline_rpc.sql` | `total_points` ??`point_balance`; `total_spent_amount` ??`lifetime_spend` in membership earn, membership detail, and dashboard paths. | `bootstrap_customer_app_v2()` calls `get_customer_membership()`, so this downstream stale reference blocked the 600120 verification path. |
| `sql/migrations/0115_create_waiting_pipeline_rpc.sql` | Pre-existing `register_waiting()` stale references corrected in the prior 짠24 round: `max_waiting_count` ??`max_wait_number`; removed nonexistent target insert references to `memo` / `pre_order_amount` in the tested path. | Required for `register_waiting()` to execute against the actual schema. |
| `sql/migrations/0116_create_customer_app_bootstrap_rpc.sql` | Removed nonexistent `store_settings.min_order_amount`; `locale` ??`preferred_locale`; `thumbnail_url` source changed to `image_url`; customer points read from `point_balance`; active waiting preorder fields hardcoded false/0 where the actual column does not exist in the tested path. | Required for customer app bootstrap to execute against the actual schema. |
| `sql/migrations/0149_create_guest_customer_bootstrap_rpc.sql` | Final live `bootstrap_customer_app_v2()` definition kept aligned with 0116 fixes and direct live re-execution. | Later `CREATE OR REPLACE` in 0149 is the live version. |

## Live Execution Procedure

The round explicitly followed the lesson from 짠24:

1. Source files were corrected.
2. Later `CREATE OR REPLACE` files were checked before deciding the live target.
3. `catchmenu_meta.migration_history` checksums were synced for in-place fixes.
4. Affected live functions were directly re-executed with `docker exec ... psql`.
5. `pg_get_functiondef()` was used to verify live function bodies.

## Boundary Notes

- No cloud database was touched.
- No git commit was performed.
- `0149` preserves `is_guest` on conflict by excluding it from the `DO UPDATE SET` list.
- `0150` only widens `chk_event_domain`; it does not change any other ledger constraint.
- The earlier `LOCAL_VERIFICATION` failure was a verification parameter mistake, not a source defect.
