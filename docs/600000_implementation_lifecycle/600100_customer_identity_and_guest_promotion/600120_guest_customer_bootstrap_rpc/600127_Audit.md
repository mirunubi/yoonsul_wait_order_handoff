# 600127_Audit.md

Status: Audited
Lifecycle: Audit
Stage: 6
Owner: Codex
Date: 2026-07-11

## Final Audit Decision

ACCEPT.

## Audit Criteria

| Criterion | Result | Evidence |
|---|---|---|
| 0149/0150 stayed inside the approved 600124 ChangeContract boundary | PASS | 0149 implements the guest customer helper and patches the two approved entry RPCs. 0150 only widens the waiting ledger event domain required by the approved runtime path. |
| §24 follow-up fixes were Human-approved before implementation | PASS | Each expansion was explicitly approved during the session: initial 0115/0116/0149 stale-column correction, 0150 `waiting` event-domain widening, and total-points/total-spent follow-up audit. |
| Live DB was actually updated, not only migration_history checksums | PASS | Affected functions were directly re-executed with `docker exec ... psql`; `pg_get_functiondef()` was used afterward. |
| Final runtime verification passed | PASS | `register_waiting()` with `p_source := 'STAFF'` returned `success: true`; `bootstrap_customer_app_v2()` completed successfully without explicit `p_customer_id`. |
| Response compatibility preserved | PASS | Existing JSON keys such as `total_points`, `total_spent_amount`, `thumbnail_url`, and `min_order_amount` were preserved where they are response contract keys; backing columns were corrected. |

## Findings

1. `0149_create_guest_customer_bootstrap_rpc.sql` correctly creates `catchmenu_store.get_or_create_guest_customer(...)`.
2. `0149` correctly keeps `is_guest` out of the `ON CONFLICT DO UPDATE SET` list, preserving promoted customers.
3. `register_waiting()` and `bootstrap_customer_app_v2()` now auto-bootstrap a guest customer when `p_customer_id` is omitted.
4. `0150_widen_event_domain_constraint.sql` was necessary because `register_waiting()` records `event_domain = 'waiting'`.
5. The apparent `LOCAL_VERIFICATION` blocker was not a code defect. The verification SQL passed an invalid `p_source`, and `p_source` is intentionally written to `caused_by_type`.
6. The final `STAFF` execution proves the approved runtime path works.

## Residual Notes

- This audit does not approve unrelated dirty worktree changes.
- This audit does not apply anything to cloud.
- This audit does not close unrelated legacy SQL residue outside the staged SQL set.

## Conclusion

The 600120 guest customer bootstrap RPC track and the Human-approved §24 live-function fixes satisfy the implementation, verification, and audit criteria.

Final status: ACCEPT.
