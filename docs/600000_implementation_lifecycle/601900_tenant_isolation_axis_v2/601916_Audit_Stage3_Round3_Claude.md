# 601916_Audit_Stage3_Round3_Claude.md

Status: Active
Lifecycle: Audit
Last Updated: 2026-09-06

## §0 수행 조건

`000701` §47.1 지정 actor 요건을 충족한다.

```text
수행        Claude (Opus), 새 대화창
사전 맥락    0
저장소 접근  없음. 첨부 9건만
결과 전달    답변 출력. Human 이 이 파일로 편입
```

**Round 3 동시 수행**

```text
Verifier A   Codex    601914   blocking 2 / informational 6
Verifier B   Cowork   601915   blocking 1 / informational 16
                               ⚠️ Round 2 와 같은 세션. 자기검토 자진 신고
Verifier C   Claude   이 문서   blocking 7 / informational 12
```

## §1 검증 결과 — VERBATIM

**아래는 답변 원문이다. 편집하지 않았다.**

````
# Round 3 · Verifier C — 외부 타당성

첨부 9건 전부 수신·통독했다. 저장소·DB 접근은 없다.

---

## Round 2 처분 확인

| # | 처분 주장 | 실제 | 판정 |
|---|---|---|---|
| **R2-1** | `TI-13` 에 격리 전이 예외 추가 | `601902` §1.13 이 「격리 상태 자체를 관리하는 전이는 이 거부의 대상이 아니다」를 본문에 두고 ⚠️ 로 범위를 전이에 한정. `601905` §2 가 `TR{"격리 상태 전이인가"}` 분기로 옮김 | **닫힘** — 단, §4 `HD-0-A-2R-11` 미동기 → `F-3` |
| **R2-2** | `TI-6` 파생 입력에서 `payload_hash` 제거 | `601902` §1.6 파생 입력 5항(`tenant_id`·`operation`·target identity·stable action identity·`policy_version`), `payload_hash` 는 대응 보존값으로 재배치. `601905` §4 `K` 노드 5항 + `PH` 비교 분기 | **닫힘** |
| **R2-3** | `TI-3` 을 `010630` §28 default 로 닫음 | `601902` §1.3 이 15 상태 열거 후 「`AUTHORITY_ALLOWED` 가 아닌 모든 상태에서 실행하지 않는다」. `601905` §3 `STOP` 노드 | **닫힘** — 단서 2건 → `IN-2` · `IN-5` |
| **R2-4** | `601905` §2 가 gate 판정을 §3 으로 넘긴다 | §2 note 는 「gate 결과에 따라 실행 여부가 정해진다 — §3」으로 바뀌었으나, **같은 §2 「읽는 법」 표에 「`AUTHORITY_ALLOWED` 일 때만 실행 / `TI-3-d`」 행이 그대로 남아 있다** | **부분** → `F-1` |
| **R2-5** | `N-7` 순환 인용 — 근거와 발견 경로 분리 | `601902` §1.12 가 「근거 — `601702` §1.27·§1.28 / 발견 경로 — `601903` §5.3·`601905` 검토 의견」으로 분리. §7 목록의 `601903`·`601905` 행도 「발견 경로」로 정정 | **닫힘** |
| **M-1** | §7 근거 목록 누락 5건 보충 | `010004` §7 · `000221` · `600010` · `601909` · `601913` 5행 전부 §7 에 존재 | **닫힘** — Round 2 자체 추가분은 미반영 → `IN-2` |
| **M-2** | `TI-14` 근거의 Stage 0 미채록 사유 명시 | `601902` §1.15 직전 ⚠️ 가 「`601901` `A1`~`A4` 분류 어디에도 속하지 않는다 / 채록 대상이 아니라는 판단이며 누락이 아니다」로 기록 | **닫힘** |
| **M-3** | `601702` §1.33 보강 채록 후 `TI-15` 인용 | `601901` §10.2 에 §1.33 전문 채록(`601702` 792~833행 원문과 일치 확인). `601902` §1.15 근거 줄에 등재 | **닫힘** |
| **M-4** | `601910` 을 `601905` 입력·근거·개정 이력에 등재 | §9 근거 목록에는 `601910` 행이 추가됐다. **§0.1 입력 목록에 없고, 개정 이력에도 `B-1`·`B-3` 정정이 기록되지 않았다** | **부분** → `IN-3` |
| **M-5** | `TI-13` 유보 4건을 `601902` §5 에 반영 | §5 에 「containment block 판정 위치 — `0-C` / Safe Projection rule / policy permission 모델 — `0-C` / surface·device context 표현」 4행 존재 | **닫힘** |
| **M-6** | `000190` §8 을 §1.15 본문에서 사용 | §1.15 본문 인용 블록에 `000190` §8 2행 존재. `000150` §12 도 `TI-15.3` 본문에 반영됨 | **닫힘** |
| **M-7** | `010640` §6 잔여 15 상태의 소관 | `OQ-6` 신설로 **잔여 15 상태**는 열렸다. 그러나 **`M-7` 전단 — `SCOPE_PARTIAL_VALID` 가 scope envelope 소관인 `TI-8` 이 아니라 `TI-2` 에 배정된 것 — 은 어디에서도 다뤄지지 않았다** | **부분** → `F-7` |
| **OQ-5** | `601702` §1.33 채록 후 인용, `Q-P14` 승계 | `601901` §14 에 `Q-P14` 신설 확인. `601902` §6 `OQ-5` 가 「해소」로 표기 | **닫힘** |

**닫힘 10 · 부분 3 · 안 닫힘 0.** 공란 0건.

---

## 종합

| 축 | 발견 | blocking | informational |
|---|---|---:|---:|
| C0 Round 2 처분 확인 | 3 | 2 | 1 |
| C1 외부 타당성 | 2 | 1 | 1 |
| C2 상위 선언 위반 | 2 | 0 | 2 |
| C3 수직 추적성 | 2 | 1 | 1 |
| C4 모델 정합 | 12 | 3 | 9 |
| **합** | **19** | **7** | **12** |

---

## Findings

### blocking

| # | 축 | 지점 | 내용 | blocking | rule 근거 |
|---|---|---|---|---|---|
| **F-1** | C0·C4 | `601905` §2 「읽는 법」 표 · §3 ⚠️ | `601913` `R2-4` 처분이 §2 note 만 고치고 **§2 「읽는 법」 표의 「`AUTHORITY_ALLOWED` 일 때만 실행 / `TI-3-d`」 행을 남겼다.** 그런데 `601905` §3 ⚠️ 는 「**§2 는 gate 결과를 §3 으로 넘기고 판정하지 않는다**」고 적는다. 문서가 자기 자신에 대해 사실과 다른 서술을 하고 있다. 실질 내용은 `R2-3` 처분 후 `TI-3` 과 일치하나, 4단계가 §2 「읽는 법」을 읽으면 근거를 `010630` §28 이 아닌 `TI-3-d`(`601903` §2 의 구판 식별자)로 잡는다 | ✅ | rule 5 · rule 6 |
| **F-2** | C4 | `601905` §0.1 · §7.4 | `601902` §6 은 `OQ-1`~**`OQ-6`** 다. `601905` §0.1 입력은 「`OQ-1` ~ `OQ-4`」, §7.4 표제는 「**`601902` §6 열린 질문 — 모델에서의 처리**」인데 4행만 있다. `OQ-6`(`010640` §6 잔여 15 상태의 소관)은 `601902` 가 `TI-2` 에 배속했고, `TI-2` 는 `601905` §1 이 그리는 규칙이다. §9 가 `601902` 를 「모델의 유일한 선언 출처」라 선언한 이상, 완결형 표제를 단 표의 누락은 선언 소실이다. **Round 1 `N-1`(입력을 `TI-1`~`TI-11` 로 유지)과 동형이 Round 2 처분에서 재발했다** | ✅ | rule 5 · rule 6 |
| **F-3** | C4 | `601902` §4 `HD-0-A-2R-11` ↔ §1.13 `TI-13` | `HD-0-A-2R-11` 은 「An isolated tenant fails that condition, so access is refused and refused closed」로 **예외 없이** 닫혀 있고 판정자 서명이 **2026-09-04** 다. `R2-1` 처분(2026-09-05)은 `TI-13` 본문에만 격리 전이 예외를 넣고 **`HD` 를 갱신하지 않았다.** `601905` §0.1 은 `TI-1`~`TI-15` 와 `HD-0-A-2R-1`~`13` 을 **동시에** 입력으로 선언한다. 같은 문서의 두 선언 블록이 `ISOLATED` 의 효과를 다르게 말하며, `HD` 쪽이 `R2-1` 이 자기 차단이라 판정한 바로 그 형태다 | ✅ | rule 5 · rule 6 |
| **F-4** | C3 | `601902` §1.11 · §7(`010004` §26) / §1.8 · §7(`010640` §5) | **`010004` §26 은 `601901` §3.1 의 관련 절 목록(`§2~§7, §19~§21, §24~§25, §27, §29`)에도 없고 §5.2 채록에도 없다.** `601902` `TI-11` 본문과 §7 근거 목록이 그것을 인용한다. 실제 출처는 `600021` §1.1 `C-1` 이며 원천 채록이 아니다. **`010640` §5 도 같다** — §7 목록과 `TI-8` 근거가 인용하나 `601901` §6.2 채록 절은 `§2·§4·§6·§7·§31·§35·§42` 로 §5 가 없다(`Q-P5` 가 문구만 언급). C3 지시가 든 전례 2건(`010650` §38 · `601702` §1.33)에 이은 **3·4번째 사례이며 둘 다 미보강 상태다** | ✅ | rule 3 |
| **F-5** | C4 | `601905` §6 · 그 하단 「출처」 | §6 은 「**`601904` §6 C5 실측을 옮긴다**」로 시작하고 하단에 「출처 — `601904` §2 C1 · §6 C5」를 명시한다. 그러나 **`601904` §2 C1 은 11행, §6 C5 도 11행이며 둘 다 `TI-1`~`TI-11` 뿐이다**(`601904` §0 이 입력을 `TI-1`~`TI-11` 로 명시). `601905` §6 의 `TI-12`~`TI-15` 4행은 그 문서에 존재하지 않는다. `TI-12`·`TI-13` 행은 `601901` 을 별도 인용하나, **`TI-14` 「격리와 과금을 잇는 객체도 0건」과 `TI-15` 「business scope 별 권한 분리를 검사하는 객체 0건 · cross-business link 의 격리 연동 객체도 0건」은 첨부된 어느 실측 문서로도 뒷받침되지 않는다.** `601904` §7 쿼리 전문에 해당 검색이 없다 | ✅ | rule 6 |
| **F-6** | C1 | `601902` §1.6 `TI-6` · `601905` §4 `PEND` | `R2-3` 은 `TI-3` 이 `010630` §6 15 상태 중 4건만 다루고 「모든 경우」로 닫은 것을 blocking 으로 잡아 **원천 default 인용으로 봉쇄**했다. **`TI-6` 에 같은 결함이 남아 있다.** `010660` §6 은 12 상태이고 `TI-6` 은 그중 `PAYLOAD_CONFLICT`·`DUPLICATE_RETURN_RESULT`·`COMPLETED` 계열 3건만 다루며 **default 봉쇄 문장이 없다.** `B-2` 처분이 만든 `601905` §4 `PEND`(「처리 중 또는 결과 불명 — Stage 4」)가 그 공백을 종단 노드로 드러낸다. 그런데 `010660` §6 은 `IDEMPOTENCY_DUPLICATE_IN_PROGRESS` 를 canonical 상태로 갖고, `TI-1` 이 구속으로 채택한 `010630` §21 은 「**Duplicate action must return existing result or route to review. It must not execute twice.**」를 요구한다(`601901` 944~948행). 모델대로 구현하면 in-progress 중복의 처분이 선언되지 않은 채 4단계 재량으로 남는다. `601902` §5 「정하지 않는 것」에도 §6 `OQ-N` 에도 없다 | ✅ | rule 3 · rule 4 |
| **F-7** | C0·C2 | `601902` §1.2 `TI-2` · §6 `OQ-6` | `M-7` 은 두 부분이었다 — ⑴ 잔여 15 상태 소관 부재, ⑵ **`SCOPE_PARTIAL_VALID` 가 scope envelope 소관인 `TI-8` 이 아니라 containment 소관인 `TI-2` 에 배정된 것.** `OQ-6` 은 ⑴만 담고 ⑵은 어디에도 없다. 더해 **원천 의미가 전용됐다** — `601901` §6.2 채록 원문은 `SCOPE_PARTIAL_VALID` = 「Scope valid only for limited operation」이고 `010640` §6 표제는 `Scope Validation State Skeleton`(요청 단위 검증 결과)이다. `TI-2` 는 이를 「scope 가 부분적으로만 확정된 상태」로 옮기고 「scoped containment representation 이 **자신의** scope 유효성 상태를 갖는다」로 지속 보유 속성화한다. `601905` §1 ⚠️ 가 그대로 옮겼다 | ✅ | rule 3 · rule 5 |

### informational

| # | 축 | 지점 | 내용 |
|---|---|---|---|
| IN-1 | C3 | `601901` 16행 | **2026-09-06 `Pass 2 보강` 행 삽입으로 `601901` 의 모든 행번호 인용이 +1 어긋났다.** 첨부본 실측 — `no containment block` 220행(인용 201), `cross-scope attempt if any` 246행(인용 245), `AUTHORITY_PARTIAL_ALLOWED` 849행(인용 848), `DENY_UNLESS_EXPLICITLY_ALLOWED` 985행(인용 984), `Same key with different payload must not execute` 1516행(인용 1515). `601902` §1.3·§1.6·§1.13, `601909` §0.1·§9, `601913` §0.2·§8 이 전부 구 행번호다. `M-3` 처분이 만든 부수 결함이다 |
| IN-2 | C4 | `601902` §7 | `M-1` 이 Round 1 추가분 5건을 보충했으나 **Round 2 추가분은 넣지 않았다** — `010630` 행은 「authority gate family · SCOPE_GATE · multi-party」로 `R2-3` 의 근거인 **§6·§28 이 없고**, `010660` 행은 「§4 · §5」로 `R2-2` 의 근거인 **§6 이 없다.** `M-1` 과 같은 유형의 재발 |
| IN-3 | C4 | `601905` §0.1 · 개정 이력 | `M-4` 부분 처분. `601910` 이 §9 에는 등재됐으나 §0.1 입력 목록에 없고, 개정 이력 어느 행도 `B-1`(§2 역방향 등치)·`B-3`(§6 실측 범위) 정정을 기록하지 않는다. 2026-09-06 행은 `R2-1`~`R2-4` 만 적는다 |
| IN-4 | C2 | `601902` §1.7 `TI-7` | `601901` §3.2 는 `601702` 관련 절을 「§1 전건, **특히 §1.22·§1.26·§1.27·§1.28·§1.40·§1.43**」으로 지목했다. `TI-12` 가 §1.27·§1.28 을, `TI-15` 가 §1.33 을 인용한다. **`TI-7` 은 §1.22(Tenant ≠ MerchantAccount)·§1.43(외부 provider 연결은 tenant/store 경계 귀속)을 인용하지 않는다.** `601903` §5.5 가 그 접점을 이미 기록했다. `Q-P14` 는 **미채록**만 추적하며 이 **미인용** 계열에는 추적자가 없다 |
| IN-5 | C4 | `601905` §3 | 「상태값은 `601901` 이 채록한 `010630` 의 **canonical 이름**이다. 이 문서가 만든 이름이 아니다」라고 적으나, 채록 원문(`601901` 845~859행)은 15 상태 전부 `AUTHORITY_` 접두를 갖는다. `601902` §1.3 · `601905` §3 의 15항 목록은 첫 항 외 14항에서 접두가 탈락했다. 같은 §1.3 의 4항 열거는 완전명을 쓴다 |
| IN-6 | C4 | `601905` §4 「읽는 법」 | 「파생 입력 5항」의 출처로 `TI-6-d` 를 든다. `601903` §2 `TI-6-d` 는 **`payload_hash` 를 포함한 6항**이다(`R2-2` 이전 판). 「`TI-6` 보강」 병기로 완화되나 식별자 자체는 구판을 가리킨다 |
| IN-7 | C4 | `601905` §5 `Q-2` | 같은 형태. `TI-10-a`(`601903` §2)는 `cross-scope attempt if any` 를 포함하지 않는 `T3-3` 이전 판이다. 「`TI-10` 보강」 병기가 있다 |
| IN-8 | C1 | `601905` §2 `CHK`·`OK` 노드 | `T3-1` 의 실질 우려는 「전이가 통제되는 상태 컬럼이 생기고 그 값을 읽는 경로는 여전히 0건일 수 있다」였다. `TI-13` 이 **효과**를 선언했으나 `601902` §5 는 「containment block 판정 위치 — `0-C`」로 강제 지점을 이월했고, `601905` §2 ⚠️ 는 「`OK` 노드 뒤를 그리지 않았다」로 확인한다. `601905` §6 `TI-13` 행이 「접근 차단을 강제하는 것은 없다」로 실측한다. **후속 나선 이월이 명시돼 있으므로 blocking 으로 올리지 않으나**, 이 나선 4단계 종료 시점에 `isolation_state` 소비자 0건이 유지될 수 있다는 사실은 그대로 남는다. `§2` `S6-1`(객체를 만들면 실제 producer·caller 가 있어야 한다)이 기존 컬럼에 적용되는지는 선언되지 않았다 |
| IN-9 | C4 | `601905` §5 ⚠️ · §8 `TI-15` 행 | §5 ⚠️ 는 「`TI-15.4` 의 link data flow 차단은 §2 `ISOLATED` 의 효과가 그것을 담는다」고 주장한다. §2 는 「`ISOLATED` 인 tenant 에 대한 요청」만 그리므로 `TI-15.4` 의 「data flow」는 덮이나 「**추론된 관계를 계속 부여하지 않는다**」는 요청 사건이 아니어서 덮이지 않는다. 또 §8 `TI-15` 행은 `TI-15.1`·`.2`·`.4` 의 위치만 적고 **`TI-15.3`(다른 business 운영으로 전파되지 않음)에 대응하는 표현을 지목하지 않는다** |
| IN-10 | C2 | `601902` §1.9 `TI-9` | `010004` §20 원문(`601901` 254~285행)은 발동 trigger 10건 · containment action 9건이다. `TI-9` 는 trigger 4건을 코드블록으로 열거하고 「`010004` §20 이 containment 의 1차 발동 사유를 정한다」로 도입할 뿐 **축약임을 본문에 명시하지 않는다.** action 쪽은 「§20 이 열거한 containment action 중 … 있다」로 축약을 인정한다. Round 1 `C2-2` 는 「`TI-9` 가 그 축약을 명시적으로 선언했기 때문에」 informational 로 뒀는데, 그 선언은 action 쪽에만 있다 |
| IN-11 | C4 | `601905` §0.3 ↔ §8 | §0.3 「이 모델에 없는 것 — **의도된 부재 3건**」(과금 요소 / 기능별 차단 목록 / cross-business link 노드)과 §8 「**미표현 3건**」(`TI-1`·`TI-11`·`TI-14`)은 서로 다른 집합인데 같은 「3건」 수사로 병기된다. 교집합은 `TI-14` 뿐이다. §0.3 이 「4단계는 §8 추적표와 함께 이 블록을 읽는다」고 잇는 만큼 계수 혼동 여지가 있다 |
| IN-12 | C0 | `601909` §7 · §8 | `601913` §7 이 「`601909` 갱신 — Round 1 처분 결과 미반영」을 처분 대기로 올렸다. §7.1 이 추가돼 처분 결과를 기록했으나 **§8 「Human 처분 대기」는 `T3-1`~`T3-8`·`N-1`~`N-6` 을 미처분으로 그대로 열거한다.** §7 은 「이 통합 시점의 판정이며 그대로 보존한다」로 명시했으나 §8 에는 그런 보존 선언이 없다 |

---

## TI-N 추적표

| `TI-N` | `601901` 근거 | `601905` 표현 | 완결 |
|---|---|---|---|
| `TI-1` | §3.1′ `A1′` 3건 · §14 `Q-P6`·`Q-P12` | §8 — **미표현**(정책 채택 지위는 상태·책임 모델 요소 아님). §6 「측정 불가」 | ○ |
| `TI-2` | §6.2 `010640` §6 16 상태 · §9.2.2 `010650` §4·§17·§18·§36 | §1 전체 · §5 `Q-9` · §6 | **△** `F-7` — `SCOPE_PARTIAL_VALID` 배정·의미 |
| `TI-3` | §9.1.2 `010630` §6(845~859행) · §28(985행) · §18 | §2 발동 note · §3 gate 전체 | **△** `F-1` — §2 「읽는 법」 잔존 |
| `TI-4` | §9.2.2 `010650` §16 release·§35(1299행)·§38(1375행) · `010630` §18 | §2 해제 note · §5 `Q-1`·`Q-7` | ○ |
| `TI-5` | §9.2.2 `010650` §35 「Release often requires stronger authority」 | §2 비대칭 다이어그램 | ○ |
| `TI-6` | §9.3.2 `010660` §4·§5·§6(1505~1516행)·§10 | §4 전체 · §5 `Q-5`·`Q-6` | **△** `F-6` — 12 상태 중 3건, default 봉쇄 없음 |
| `TI-7` | §6.2 `010640` §4 `merchant_id` · §8.2 `000170` §3·§4 | §5 `Q-10` 점선 노드 | ○ 단 `IN-4`(`601702` §1.22·§1.43 미인용) |
| `TI-8` | §6.2 `010640` §2·§42. **§5 채록 없음** — §14 `Q-P5` 가 문구만 언급 | §5 `Q-2`·`Q-3` | **△** `F-4` |
| `TI-9` | §5.2 `010004` §20(254~285행) | §5 `Q-8` → `Q-1` 실선, `Q-9` 점선 | ○ 단 `IN-10`(10→4 축약 미명시) |
| `TI-10` | §5.2 `010004` §19(232~246행) · Pass 2 §17~§20 | §5 `Q-2` · `Q-1` | ○ |
| `TI-11` | §5.2 `010004` §24 11항(287~299행). **§26 채록 없음 · 관련 절 목록에도 없음** | §8 — **미표현**(Stage 7 게이트) · §7.2 `P-10` | **△** `F-4` |
| `TI-12` | §10.2 `601702` §1.27·§1.28 | §1 Mermaid 소유 라벨 · §1 ⚠️ 2건 · §6 | ○ |
| `TI-13` | §5.2 `010004` §7(202~224행) · §17.1·§17.2 실측 | §2 「`ISOLATED` 의 효과」 다이어그램 + 전이 예외 · §6 | **△** `F-3` — §4 `HD-11` 과 불일치 |
| `TI-14` | **채록 없음.** `601902` §1.14 ⚠️ 가 채록 대상 아님으로 선언(`M-2` 처분) | §8 — **미표현**(과금 노드 부재가 곧 표현) · §5 ⚠️ · §6 | ○ 단 §6 행 무근거 → `F-5` |
| `TI-15` | §7.2 `000150` §12·§22·§23 · §9.2 `000190` §3·§8·§10·§17·§20·§27 · §10.2 `601702` §1.33 | §3 `SCOPE` 노드·⚠️ · §5 ⚠️ · §6 | ○ 단 `IN-9`(`TI-15.3` 미지목) · §6 행 무근거 → `F-5` |

**15건 전부 기록. 공란 0건.**

---

## 확인 불가 항목

첨부에 없어 판단하지 못한 것이다. **추정으로 채우지 않았다.**

| # | 항목 | 영향받는 판정 |
|---|---|---|
| U-1 | `601900_Readme_Tenant_Isolation_Axis_V2.md` 원문 | `601902` §1.11 「`601900` Readme §4 의 4번 구속」 · §3 「Readme §5 승계 finding」 · `601905` §9 「§3 착수 순서 · §4 구속」 · `601909` `N-3`(File List 가 `601902` 를 「11건 · `HD-1`~`9`」로 기록) — 전부 검증 불가 |
| U-2 | `010004`·`010640`·`010630`·`010650`·`010660`·`000150`·`000170`·`000190` 원문 | `601901` 채록본만 있다. **채록의 정확성·완전성을 대조하지 못한다.** `F-4` 는 「`601901` 이 채록하지 않았다」까지만 성립하며, `010004` §26 · `010640` §5 의 실제 내용이 `TI-11`·`TI-8` 을 지지하는지는 판정 불가 |
| U-3 | `601906`~`601908` · `601910`~`601912` 원본 | `601909`·`601913` 의 요약만 있다. `R2-1`~`R2-5`·`M-1`~`M-7` 의 **원 지적 범위**가 통합자 요약과 같은지 확인 불가. `F-1`·`F-7` 의 「부분」 판정은 통합 문서 기재 범위 기준이다 |
| U-4 | `000221` §4.1 · `600010` §1.1 | `TI-14` 의 Human Gate A 근거. 게이트가 실제로 닫혔는지, `TI-14` 5항이 게이트가 요구한 결정을 덮는지 판정 불가 |
| U-5 | `000701` §13.6 · §46 · §47.1 · §48 | 3단계 절차 자체의 준수 여부 · `finding acceptance rule` 원문(`601909` §5 ⚠️ 가 저장소 미편입으로 기록) 확인 불가 |
| U-6 | `601816` finding 15건 원문 | `601902` §2 의 재적용 10건 / 승계 5건 분류의 타당성 판정 불가. `S6-3` 의 원 서술이 `R2-1` 의 대비와 같은지도 확인 불가 |
| U-7 | 라이브 DB · 저장소 | `601903`·`601904` 의 실측치를 재현하지 못한다. `F-5` 는 「첨부 문서로 뒷받침되지 않는다」까지만 성립하며 「측정 결과가 틀렸다」가 아니다 |
| U-8 | `601901` baseline commit `e6573af…` | 채록이 그 시점 blob 과 일치하는지 확인 불가 |
| U-9 | `601905` Mermaid 렌더 결과 | 소스 텍스트로만 판단했다. 렌더 시 노드·간선이 의도대로 보이는지는 확인하지 않았다 |
| U-10 | `010004` §21·§25·§27, `010640` §5·§9·§15·§29·§30·§32~§34·§36~§40 | `601901` 이 **관련 절로 지목했으나 원문을 옮기지 않은** 절이다. 이 나선 선언이 그 절들과 어긋나는지 판정 불가 |

---

**우려 없음으로 판정한 지점**(침묵하지 않는다): `TI-4`·`TI-5` 의 발동/해제 비대칭은 `010650` §35·§38 과 `010630` §18 에 대해 정합하며 `601905` §2 가 정확히 옮겼다. `TI-10` 은 `T3-3` 처분 이후 `010004` §19 14항을 빠짐없이 담고 `601905` §5 `Q-2` 가 전건 옮겼다. `TI-12` 는 `601702` §1.27·§1.28 과 완전 일치한다. `R2-1`·`R2-2`·`R2-3`·`R2-5`·`M-1`·`M-2`·`M-3`·`M-5`·`M-6`·`OQ-5` 10건은 실제로 닫혔다.
````

## §2 Status

```text
Audit evidence only.
```

**통합은 `601917` 이 수행한다.**
