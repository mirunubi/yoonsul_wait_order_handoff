# 601913_Report_Stage3_Round2_Integration.md

Status: Active
Lifecycle: Report
Last Updated: 2026-09-05

## §0 이 문서의 지위

`000701` §47.1 **3단계 Round 2 통합**이다.

```text
Verifier A   Codex    601910   실측 축      blocking 3 / informational 6
Verifier B   Cowork   601911   문서 축      blocking 1 / informational 12
Verifier C   Claude   601912   외부 타당성   blocking 4 / informational 16 / 확인 불가 8
```

**세 검증자가 동시에 수행했고 서로의 결과를 참조하지 않았다.**

### §0.1 Round 1 과의 관계

```text
Round 1   601906 ~ 601909   blocking 8 · informational 21
처분      TI-13 · TI-14 · TI-15 신설
          TI-2 · TI-3 · TI-6 · TI-10 · TI-12 보강
          601905 재동기화 · N-1 ~ N-7
```

> ⚠️ **`601909` §7 · §8 은 아직 「blocking 8건 미해소 · Human 처분 대기」로 적혀 있다.**
> **처분 후 갱신되지 않았다.**
> **세 검증자 모두 그 사실을 관측했고 판정을 승계하지 않았다.**

**Round 1 처분 확인 — 3주체 종합**

```text
Codex   닫힘 13 · 부분 2   T3-8 · N-3
Cowork  닫힘 13 · 부분 2
Claude  N-7 을 「안 닫힘」으로 판정 — B-C-4
```

### §0.2 통합자 직접 재도출 — 3건

**`000701` §13.6 에 따라 원문에서 확인했다.**

| 주장 | 실측 | 결과 |
|---|---|---|
| `010660` §6 이 `Same key with different payload must not execute` 를 규칙으로 둔다 | `601901` 1509 · 1515행 | **성립** |
| `010630` §6 authority state 가 15개이고 §28 이 `DENY_UNLESS_EXPLICITLY_ALLOWED` 를 default 로 둔다 | `601901` 844 · 984행 | **성립** |
| `601905` §2 가 「gate 결과 `AUTHORITY_ALLOWED` 일 때만 실행」을 유지한다 | `601905` 186행 | **성립** |

## §1 종합

```text
보고   Codex 3 · Cowork 1 · Claude 4        blocking 8건
       Codex 6 · Cowork 12 · Claude 16      informational 34건
       Claude 확인 불가 8건

Codex blocking 3건은 2026-09-05 정정 완료 — B-1 · B-2 · B-3
미처분 blocking 5건
```

**Stage 3 Round 2 = INCOMPLETE.**

**Codex blocking 3건 — 2026-09-05 정정 완료**

| # | 내용 | 정정 지점 |
|---|---|---|
| `601910` `B-1` | `601905` §2 가 `containment block 없음 = isolation_state NONE` 로 역방향까지 등치 | §2 Mermaid · 읽는 법 · ⚠️ |
| `601910` `B-2` | `601905` §4 가 재요청과 처리 완료를 동일시 | §4 Mermaid 2단 분기 · ⚠️ |
| `601910` `B-3` | `601905` §6 이 「격리 상태 참조 제약 0건」으로 실측 범위를 바꿈 | §6 `TI-13` 행 |

> ⚠️ **`601910` `B-2` 정정이 `601912` `B-C-1` 을 해소하지 않는다.**
>
> ```text
> B-2     601905 §4 작도 결함 — 재요청과 완료의 분기 부재
> B-C-1   TI-6 선언 결함 — payload_hash 가 key 성분이면
>         010660 §6 의 PAYLOAD_CONFLICT 가 성립하지 않는다
> ```
>
> **전자는 2단계 작도이고 후자는 1단계 선언이다.**
> **`R2-2` 가 후자를 담는다.**

## §2 미처분 blocking — 5건

| # | 내용 | 출처 | 소관 |
|---|---|---|---|
| **R2-1** | **`TI-13` 이 예외 없이 선언해 해제 경로가 자기 차단된다.** `ISOLATED` 인 tenant 의 tenant-scoped object 접근이 거부되는데, `TI-4` 의 해제 전이가 그 tenant 를 대상으로 하는 행위다. `601905` §2 도 `ISOLATED` → 거부 단일 경로다. **`601902` §2 가 `S6-3`(안전한 공식 release 경로가 실행 가능해야 한다)을 재적용 10건에 넣었는데 그것이 성립하지 않는다** | Claude `B-C-2` | **1단계** |
| **R2-2** | **`payload_hash` 가 canonical key 성분이면 `010660` §6 을 통과시킨다.** payload 가 달라진 재요청은 다른 key 가 되어 `601905` §4 에서 「아니오 → 처리」로 흐른다. `010660` §6 은 `Same key, different payload` 를 `IDEMPOTENCY_PAYLOAD_CONFLICT` 상태로 두고 실행 금지를 요구한다. **conflict 를 탐지하려면 payload 가 key 성분이 아니어야 한다** | Claude `B-C-1` · 통합자 재도출 | **1단계** |
| **R2-3** | **`TI-3` 이 authority state 4개만 열거하고 「모든 경우」로 닫았다.** `010630` §6 은 15 상태다. 11개 처리가 `601902` · `601905` · `OQ-N` 어디에도 없고 `601905` §3 에 default 간선도 없다. `010630` §28 이 `DENY_UNLESS_EXPLICITLY_ALLOWED` 를 default 로 두나 두 문서 모두 §28 을 인용하지 않는다 | Claude `B-C-3` · 통합자 재도출 | **1단계** |
| **R2-4** | **`601905` §2 186행이 `OQ-1` 을 확정한다.** 「gate 결과 `AUTHORITY_ALLOWED` 일 때만 실행」이 `AUTHORITY_PARTIAL_ALLOWED` 를 미실행으로 못박는다. 같은 문서 §3 은 「어느 쪽으로 잇든 `601902` 에 없는 선언」이라며 간선을 그리지 않았다. **`T3-7` 처분이 §3 에만 적용되고 §2 에는 적용되지 않았다** | Cowork `BR2-B1` · 통합자 재도출 | 2단계 |
| **R2-5** | **`N-7` 이 설명만 추가하고 대상을 고치지 않았다.** `601902` §1.12 근거 줄이 `601903` · `601905` 를 근거로 열거하고, 두 줄 뒤 ⚠️ 가 그것을 부정한다. §7 근거 목록도 `601905` 를 「`TI-12` 근거」로 기록한다. 반면 `601905` §9 는 `601902` 를 「유일한 선언 출처」라 한다 | Claude `B-C-4` | 문서 정합 |

## §3 근본 원인

**`R2-1` · `R2-3` 이 같은 형태다.**

```text
선언이 「모든 경우」 · 「예외 없이」로 닫혔다
그런데 원천이나 다른 TI-N 이 요구하는 경우를 다루지 않는다
```

```text
TI-13   예외 없이 → TI-4 해제 경로를 배제
TI-3    모든 경우 → 010630 15 상태 중 11개 미처리
```

> ⚠️ **완결 주장은 그 범위를 실제로 덮을 때만 참이다.**
> **덮지 못하면 4단계가 선언되지 않은 예외를 스스로 만들게 된다.**

**`R2-1` 은 `601800` `S6-3` 의 재발이다.**

```text
601800   공식 해제 경로가 승인 플래그를 참으로 만들 조작이 없어 영구히 성공 불가
601900   해제 경로가 격리 자체에 막혀 실행 불가
```

**형태는 다르나 결과가 같다.**

> ⚠️ **「`R2-1` 은 `601800` `S6-3` 의 재발이다」는 통합자 판단이다.**
>
> ```text
> 601912 B-C-2    601902 §2 가 S6-3 을 재적용했는데 성립하지 않는다
>                 601800 의 옛 사례와 대비하지 않았다
>
> 통합자 추가      600021 §1.1 이 기록한 601800 finding 서술과 대비
> ```
>
> **3주체 어느 보고서에도 없는 판단이므로 출처를 밝힌다.**

## §4 informational — 34건

**세 보고서 원문에 있다. 중복 제거하지 않고 출처만 기록한다.**

```text
601910   informational 6
601911   informational 12
601912   I-1 ~ I-16
```

**그중 즉시 처리 가능한 문서 정합 7건**

| # | 내용 | 출처 |
|---|---|---|
| M-1 | `601902` §7 근거 목록이 `010004` §7(`TI-13`) · `000221` · `600010`(`TI-14`) · `601909`(신설 3건과 보강 5건 전건의 근거)를 누락 | Cowork `BR2-I1` / Claude `I-2` |
| M-2 | `TI-14` 의 근거 `000221` · `600010` 이 `601901` 채록 범위 밖. `601901` §0 이 선언한 「원천 → DB → Human Rule」 순서에서 `TI-14` 만 1단계가 0단계를 앞선다 | Cowork `BR2-I3` / Claude `I-1` |
| M-3 | `601702` §1.33(cross-business link 는 참조이며 권한이 아니다)이 Stage 0 채록에 없고 `TI-15` 가 인용하지 않는다. **`TI-12` 신설 경위와 같은 유형이며 `600021` §1 사유 2가 `601800` 권위보류 사유였다** | Cowork `BR2-I4` / Claude `I-4` |
| M-4 | `601905` 본문 3곳이 `601910` `B-1`·`B-2`·`B-3` 을 인용하나 `601910` 이 §0.1 입력 목록 · §9 근거 목록 · 개정 이력 어디에도 없다 | Claude `I-3` |

**`601912` 가 「닫힘 — 단서 있음」으로 판정한 단서.**

| # | 내용 | 출처 |
|---|---|---|
| M-5 | `T3-1` — `TI-13` 이 유보한 4건(containment block 판정 위치→`0-C`, Safe Projection rule, policy permission 모델→`0-C`, surface·device context 표현)이 `601902` §5 「이 나선이 정하지 않는 것」 목록에 없다 | Claude `I-9` |
| M-6 | `T3-4` — `000190` §8 은 §7 목록에만 있고 §1.15 본문 미사용 | Claude |
| M-7 | `T3-6` — `601901` §6.2 의 `010640` §6 은 16 상태 Scope Validation State Skeleton 이다. 그중 `SCOPE_PARTIAL_VALID` 1건만 사용됐고, 그것이 scope envelope 소관인 `TI-8` 이 아니라 containment 소관인 `TI-2` 에 배정됐다. 남은 15 상태의 소관 부재 | Claude `I-12` |

> ⚠️ **`T3-7` · `T3-8` 의 단서는 이 표에 넣지 않았다.**
>
> ```text
> T3-7 단서   010630 §6 15 상태 중 4건만 다룸 — B-C-3 → R2-3
> T3-8 단서   010660 §6 PAYLOAD_CONFLICT 부재 — B-C-1 → R2-2
> ```
>
> **둘 다 §2 미처분 blocking 으로 이미 올라 있다.**
> **문서 정합 informational 로 옮기면 등급이 낮아지고 이중 계상이 된다.**

## §5 확인 불가 — 8건

**Verifier C 가 저장소 · DB 접근 없이 판단하지 못한 것을 기록했다.**

**`601912` §확인 불가 원문을 따른다. 이 문서가 추정으로 채우지 않는다.**

## §6 Stage 3 Round 2 판정

```text
Stage 3 Round 2 = INCOMPLETE

blocking 8건   정정 완료 3 · 미처분 5
601905 는 현 상태로 4단계 착수 대상이 아니다
```

## §7 Human 처분 대기

**이 문서는 판정하지 않는다.**

```text
R2-1 · R2-2 · R2-3   1단계 소관 — TI-N 보강이 필요하다
R2-4                 2단계 정정
R2-5 · M-1 ~ M-4     문서 정합
601909 갱신          Round 1 처분 결과 미반영
```

> ⚠️ **`R2-1` 은 `601800` `S6-3` 의 재발이다.**
> **선언이 「예외 없이」로 닫히면 그 예외가 필요한 경로가 막힌다.**

## §8 근거 문서 목록 (`000701` §46)

| 문서 | 인용 | 지위 |
|---|---|---|
| `601910_Audit_Stage3_Round2_Codex.md` | 전문 | ACTIVE |
| `601911_Audit_Stage3_Round2_Cowork.md` | 전문 | ACTIVE |
| `601912_Audit_Stage3_Round2_Claude.md` | 전문 | ACTIVE |
| `601901` | 844 · 984 · 1509 · 1515행 — §0.2 재도출 | ACTIVE |
| `601902` · `601905` | 검증 대상 전문 | ACTIVE |
| `601909` | Round 1 통합 — §0.1 | ACTIVE |
| `010660` | §6 | ACTIVE |
| `010630` | §6 · §28 | ACTIVE |
| `010004` | §7 | ACTIVE |
| `601702` | §1.33 — `M-3` | ACTIVE |
| `000701` | §13.6 · §46 · §47.1 | ACTIVE |
| `601800` 대역 | `S6-3` 재발 사실만 — §3 | ⛔ **AUTHORITY SUSPENDED** |
