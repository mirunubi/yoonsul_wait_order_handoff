# 600901_ChangeHistory.md

Per `000701` §30 — single running file, append-only. One row per change.

| Date | Change Description | Reason/Evidence | Outcome | Linked Audit/Test |
|---|---|---|---|---|
| 2026-07-14 | `workpacket_renumbering_to_domain_ranges`: renumbered 9 workpacket folders and 53 files from their legacy `600400`-domain-era numbers to the new domain-range scheme (`600510`/`600610`/`600620`/`600710`/`600720`/`600810`/`600820`/`600910`/`600540`), following up on `600520_domain_folder_reorganization`'s earlier folder-only move (which had kept workpacket numbers unchanged) | Achieving the project's stated "전역 유일성 확보" (global workpacket-number uniqueness) goal required renumbering, not just relocating, workpackets — the folder-only move had left numbers still tied to their original `600400`-domain positions | ACCEPT — `600921`–`600924`(Overview/Logic/TestPlan/ChangeContract)/`600925_Module.md`(Stage 4)/`600926_Verification.md`(Stage 5, Claude Code + Antigravity dual independent verification, Antigravity's specific line citations cross-checked and confirmed accurate; Codex contributed a discovery-and-fix cycle for 3 design-time-nonexistent files' stale cross-references, recorded honestly as partial rather than a third full report)/`600927_Audit.md`(Stage 6) all complete. Mid-design, the `mark_payment_uncertain_overload_ambiguity` workpacket's originally-intended domain-slot number was found to collide with the already-ACCEPT'd `domain_folder_reorganization` workpacket's own number (`600520`); it was retargeted to `600540` instead — caught before implementation, resolved via Human decision, and recorded as a positive `000701` §44.2 example. `000005`/`000007` backfilled: 42 pre-existing entries renumbered, 7 new entries added for `mark_payment_uncertain_overload_ambiguity`(now Stage 6 ACCEPT, unlike DID which remains excluded pending its own future Stage 6). This is this domain folder's (`600900_cross_domain_reconciliation`) first `ChangeHistory` entry — its own `600901_ChangeHistory.md` was created as part of this workpacket's closure, resolving `600524_ChangeContract_Domain_Folder_Reorganization.md` §6.1's open question for `ChangeHistory` (domain-level `DecisionLog` remains a narrower, separate residual). 3 Open Items carried forward: (a) `600404_PlaceTakeoutOrder_Defect_Roadmap.md`'s post-split domain fit still undecided, (b) `600820`(DID)'s future index backfill deferred to its own eventual Stage 6 workpacket, (c) domain-level `DecisionLog.md` narrower residual | `600921`–`600927` (Overview/Logic/TestPlan/ChangeContract/Module/Verification/Audit) |
