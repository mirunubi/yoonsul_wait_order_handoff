# 601427 Report — Fable Design Integrity Inspection slice_04_customer_handoff_policy

- Program: `601400_fable_design_integrity_inspection`
- Domain: `domain_01_customer_handoff` / `slice_04_customer_handoff_policy`
- Input package: `601424_Slice_Input_Package_slice_04_customer_handoff_policy.md`
- Review date: 2026-07-19
- Preservation rule: §9.1–§9.9 below is verbatim Fable output — do not edit in place; Pass C disposition only in separate artifacts

---

9.1 — Master Inspection Summary
Slice: `slice_04_customer_handoff_policy` (Customer Handoff "top policy" layer).
Files inspected: 73 Markdown (read in full via 6 parallel read-only passes) + 3 SQL migrations (`0081`, `0109`, `0116`, read in full). JSON: none in slice.
Not read / excluded: none skipped; a few referenced-but-external docs (e.g. `009020`, `900162`) noted where cross-refs land outside the slice.
Domain classification (§6.2) — what this layer actually is: three sub-domains — (a) 005010 customer-handoff-flow policies (link/token security, web/native/whitelabel session, guest↔account identity, membership/loyalty, support, privacy, pilot governance), (b) 005100/005200/005400 readiness + provider/kiosk + POS-waiting-sync, (c) 900000 patent/handoff package (late-binding pipeline + 4 channels + kiosk/DID + 여전법 payment policy + a large future/vision tail).
Reference-graph reconstruction (§6.2): the intended top-of-stack order is
`005027 (payment-gate authority) + 005060 (reservation/preorder ordering) + 900170 (여전법) + 900101/900161 (Patent-2 late-binding) → channel/kiosk session policies → readiness gates → SQL`.
The layer names almost no hard SQL contract — its implementable content is state-enum vocabularies plus three genuine binding contracts (900170 여전법 card-data, Patent-2 `HOLD→COMMITTED`, 005151 Toss gating).
Finding totals: 16 findings — 1 CRITICAL, 6 HIGH, 6 MEDIUM, 3 LOW/OUT-OF-SCOPE.
Confidence & limits: MD conflicts are HIGH confidence (exact file:line on both sides). SQL "phantom/runtime" findings are RUNTIME-BLOCKER CANDIDATES — Fable is read-only and cannot execute SQL or query live schema, so crash-certainty is inferred from the schema dump carried in the sibling waiting-order slice, not observed. The single biggest limitation: not one of the 73 docs carries a reliable ratified Status marker, so "current vs superseded" is inferred from numbering + cross-references (see CH-F12).
9.2 — Structural Defect Register
CH-F08 — Systematic −10 stale self-references + triple numbering scheme (005012–005020). `STRUCTURAL DEFECT` · MEDIUM
Every Baseline-Dependency block in 005013–005019 cites `005002–005008` for docs that actually exist as `005012–005018` (e.g. `005013:44` cites `005002_...Link_Token`; `005019:44-56` cites `005002`–`005008`). A parallel legacy `06xxx` scheme appears in prose (`005013:46` "06550…"; `005018:49` "06600…"). `005020` adds broken paths: `005020:93` → `09000_data_model_state_machine/009020_Handoff_State_Machine.md` (real: `009000…/009020_Spec_Handoff_State_Machine.md`); `005020:127` cites a `005060_SOP_Entrance_...` that does not exist (005060 is the Reservation Readme) — number 005060 is effectively double-booked. Impact: three inconsistent id schemes for the same documents; dependency chains are unresolvable as written. Resolution options: (a) Owner-directed one-pass renumber-reference sweep, or (b) add an id-crosswalk table. Confidence: HIGH.
CH-F12 — No ratified Status marker on any of 73 docs; two numbering conventions co-exist. `STRUCTURAL DEFECT` / root-enabler of canonical ambiguity · MEDIUM
005-series docs carry no Status/Version/Date block at all; 900-series carry `Status: In_Progress, Owner: TBD` uniformly (even ratified-looking contracts). Bodies reference `05xxx` short-form while filenames are `005xxx`; the 900-series `Lifecycle:` header mislabels several Policy files as "Logic" (F-cluster). Impact: currency/precedence cannot be read off any single file — the direct cause of CH-F04 and CH-F05. Owner decision required: adopt a mandatory front-matter Status/Supersedes stamp for policy docs. Confidence: HIGH.
CH-F15 — Intra-document enum/value inconsistencies (cluster-local). `STRUCTURAL DEFECT` · LOW
`900131` `transfer_status` is spelled two ways (`PENDING/RETRYING` at :97 vs `HOLD_INTERNAL/TRANSFERRED/FAILED` at :161-176); `900111:180` adds a `PAID` recovery state absent from `900110:219-224`; the session-storage key `catchmenu_session_id` is mandated by `900110:98`/`900121:361`/`900141:44` yet `900141:109-111` (DROP-H) forbids exactly that shared key and requires `cm_app_session_id`/`yoonsul_session_id` — the collision the doc tries to prevent is baked into its siblings; `005017` customer-facing `Payment Review` (:328) has no internal-lifecycle counterpart (internal uses `Finance Review` :366). Confidence: HIGH.
9.3 — Design Conflict Register
CH-F02 — No-show / cancel KDS fate: CANCELLED vs HOLD (design authority vs contracts, and it maps to live SQL). `DESIGN CONTRADICTION` · HIGH
Authority Logic `900101:73` (+:256,:278): `HOLD → cancel_waiting()/mark_no_show() → CANCELLED`. Every contract/test says the opposite: `900102:430` "NO_SHOW/CALL_EXPIRED → KDS HOLD 유지"; `906010:410`; `900103:300`; `906000:304`. This is not academic — the live waiting-order SQL (`apply_no_show_transition`, `NO_SHOW_GRACE` 15-min HOLD; slice03) keeps tickets HOLD, siding with the contracts and contradicting the patent Logic. Owner decision required (it is a business-policy fork, per operational-plan §5.2). Resolution options: (a) amend 900101 to HOLD-with-grace (match implementation), or (b) amend contracts+SQL to CANCELLED. Confidence: HIGH.
CH-F03 — Order "confirmed" before vs after payment: 005027 vs 005060 (SQL follows 005060). `DESIGN CONTRADICTION` · HIGH
`005027:50` sequences `… → PAYMENT GATE → order_accepted → pos_handoff → kds_handoff` (accepted-order is post-gate; no pre-payment CONFIRMED state defined). `005060:324-331` sequences `STORE_CONFIRMED → PAYMENT_PENDING → PAID` (confirmed before payment). Live `place_takeout_order` (0081) writes `order_status='CONFIRMED'` + `session_status='ORDER_CONFIRMED'` before payment (`next_step='AWAIT_PAYMENT'`) — matching 005060, drifting from 005027. Because 005027 never defines `order_accepted`, the two payment authorities disagree with each other and the SQL had to pick one. Owner decision required: designate one sequencing authority. Confidence: HIGH.
CH-F10 — Guest-identity operation named three ways: "upgrade" / "merge" / "claim". `DESIGN CONTRADICTION` / `CANONICAL AMBIGUITY` · MEDIUM
`005014:199` "Guest-To-Account Upgrade"; `005015:124` "Guest-To-Account Merge"; `005016:138` "Guest Order Claim" (with `005016:162` implying claim may attach an order without full benefit rights — i.e. possibly a weaker, distinct op). Trigger-lists and preserve-lists also differ (005014 6+8 items vs 005015 6+11 items). Impact: downstream SQL gets 2–3 candidate event names for what may be 2–3 distinct operations. Confidence: HIGH.
CH-F11 — KDS `HOLD`/`conditions_met` mechanism has no basis in the customer-flow policy authority; keys mis-counted; delivery bypass un-contracted. `DOCUMENT–SQL DRIFT` / `MISSING DESIGN CONTRACT` · MEDIUM
`005027`, `005040`, `005050` never mention a KDS `HOLD` state or `conditions_met.payment_confirmed` — the entire payment-gate mechanism the SQL implements is a downstream invention relative to the flow-policy layer. Within the patent layer, Logic defines 5 keys at HOLD / 6 at COMMITTED (`900101:83-99`), not "7"; only `payment_confirmed` is ever tested; the **delivery `order_source IN (BAEMIN/YOGIYO/COUPANG) → immediate COMMITTED, no HOLD** bypass (`900101:104-108`) is defined solely in Logic, is untested/uncontracted, and sits in tension with INV-001's blanket "HOLD→COMMITTED only after APPROVED" (`900102:109-112`). Also `PRE_ORDER`is used as a ledger`to_state` (`900100:168`, `900101:442`) yet is undefined as a `session_status` in the same Logic's state machine (`900101:29-53`). **Confidence:** HIGH.
9.4 — Canonical Ambiguity Register
CH-F04 — Provider cutline contradiction: 005191 (Toss+PAYCO) vs 005241 (Toss+OKPOS); drift propagated. `CANONICAL AMBIGUITY` / `DESIGN CONTRADICTION` · HIGH
`005191:12` "only Toss and PAYCO are active provider candidates"; OKPOS = phase-2 investigate-only (:156). `005241:66-68` revises to first-phase Toss+OKPOS, PAYCO demoted to secondary (`005241:458-460`). OKPOS and PAYCO swap in/out of MVP. 005191 carries no superseded marker; the only supersession sentences live in `005241:433-437` (different folder; use short-form "05190"/"05240" that never restate real filenames). Drift propagates: `005201:247-248`, `005211:320`, `005221:77`, `005231:88-97` all still cite the superseded 005191 (Toss+PAYCO). Downstream deferral records (`005141 §15`) key off "the MVP cutline" — i.e. built on a disputed base. Owner decision required: ratify 005241 and stamp 005191 superseded; re-point the kiosk bundle. Confidence: HIGH.
CH-F05 — Duplicate ChangeContract/TestPlan pairs: 900102/900103 vs 906000/906010, no supersession. `CANONICAL AMBIGUITY` · HIGH
900102/900103 are concrete (name real files `0136_patch_release_kds_idempotency.sql`, `toss-payments-confirm/webhook`; `900102:177-186`), cross-referenced by 900100/900101/900170, and gate-wired (`900103:551-552`). 906000/906010 are generic templates ("Allowed files: TBD" `906010:302-313`; "Example placeholders only" `906010:445-446`), carry zero Status metadata, and are referenced by no document (orphaned). Lean: 900102/900103 canonical. But not deletable: 906000 uniquely carries a full §2 In/Out-of-Scope (`906000:11-42`) and a `READY→COOKING` forbidden-transition (`906000:82`); 906010 uniquely allows an amount-mismatch read-only check (`906010:230-256`) that 900102 forbids. Also intra-pair drift: SEATED terminal states differ (`900103:49` vs `906000:66`), audit field `created_at` vs `timestamp` (`900103:419` vs `906000:393`). Owner decision required: explicit supersession + migrate the unique 9060xx content into the canonical pair. Confidence: HIGH.
CH-F13 — Redefined / duplicate customer-app RPCs across 0081↔0116. `CANONICAL AMBIGUITY` / `DOCUMENT–SQL DRIFT` · MEDIUM
0081 creates `bootstrap_customer_app`; 0116 creates a second `bootstrap_customer_app_v2` (which is canonical for the app?). `get_customer_home` is defined by 0081 `(tenant, customer, store DEFAULT null, locale)` then dropped+recreated by 0116 with a changed signature/order `(tenant, store, customer, locale)` and a rewritten body. Order tracking exists twice: `track_takeout_order` (0081) vs `get_order_tracking` (0116). No doc states which the app calls. Confidence: HIGH (from concat).
9.5 — MD–SQL–JSON Drift Register
CH-F01 — [see 9.7] 0081 deferral of `order_sessions.customer_id` silently bypassed by 0116 + phantom columns. `DOCUMENT–SQL DRIFT` / `RUNTIME BLOCKER CANDIDATE` · CRITICAL (detailed in 9.7).
CH-F06 — Two different session tables: `order_sessions` (channel docs) vs `customer_app_sessions` (this slice's SQL). `DOCUMENT–SQL DRIFT` · HIGH
All four channel docs + 900150 model the customer session on `catchmenu_pos.order_sessions` — waiting-lifecycle states (`WAITING/ARRIVAL_PENDING/SEATED/(PAID)/COMPLETED/CANCELLED/NO_SHOW`), ~2h server expiry (`900110:126`), 15-min call→NO_SHOW (`900110:130`), guest = `customer_id IS NULL`, guest→member via `UPDATE order_sessions SET customer_id … WHERE phone_hash=… AND customer_id IS NULL` (`900121:290-292`). The slice's SQL (0081/0116) instead creates `catchmenu_store.customer_app_sessions` — `session_status ∈ {ACTIVE,EXPIRED,LOGGED_OUT}`, `expires_at = now()+30 days`, `is_guest` boolean. `customer_app_sessions` appears 0 times across the entire 900000 package; the docs' `WAITING/…` states, 30-day lifetime, and `is_guest` have no counterpart on either side. Interpretation: two genuinely different concepts (waiting/order-position ledger vs app/auth-session ledger) with overlapping "customer session" naming — a Source-of-Truth split the policy layer never reconciles. Owner decision required: declare the canonical customer-session table(s) and their relationship. Confidence: HIGH.
CH-F09 — No numeric TTL anywhere (token, cart, coupon-reservation, guest session, retention). `MISSING DESIGN CONTRACT` · MEDIUM
Security doc 005012 gives only "short"/"longer" (`005012:255,265`, no minutes); guest session "scoped, expiring" with no duration (`005013:121`); coupon "Reserved… must not remain stuck… without expiration" but no window (`005016:260`); retention "durations require legal/compliance review" (`005018:374`). Every expiry the SQL must set (token, cart, coupon hold, session, retention) is downstream-arbitrary. Impact: the security/abuse-prevention policy is unenforceable as written; different implementers will pick different TTLs. Confidence: HIGH.
9.6 — Historical / Superseded Candidate Register9.6 — Historical / Superseded Candidate Register
CH-F14 — 12 future/vision docs co-located with the active pipeline (reader-trap). `HISTORICAL OR SUPERSEDED CANDIDATE` / `OUT-OF-SCOPE OR FUTURE` · LOW (but flag prominently)
FUTURE/VISION, must not be read as current SQL contracts: `900162` (Level-1 abandoned, Level-2 MVP-target not built, `900162:337/369`), `900164/900165/900171` (Slot Container Agent — `900165:492` unchecked Phase-1 boxes), `900172` (coupon platform Phase 3/4), `900176` (mini-HACCP Phase 1-3, `신규 테이블`), `900177` (AI gateway Phase 1-4), `900178` (pricing — all `ADD COLUMN IF NOT EXISTS`, not yet in schema), `900173/900174/900175/900180` (multi-brand/Asia/investor-pitch, timelines 2028–2033+). Reader-trap: these share the folder, the `In_Progress` status, and realistic `CREATE TABLE`/RPC SQL with the active 900100-series; scope must be judged from roadmap/phase language, not the `Lifecycle:` header (which mislabels several as "Logic"). Also latent-superseded: 005191 (by CH-F04); 900179 (2026-06-21) possibly by 900163 (2026-06-26) — neither declares it.
Channel phasing (E-cluster): MVP = Ch1 web + Ch2 native (anon shell); FUTURE = Ch4 Yoonsul membership (Phase 2, `900140:84-88`), Ch3 whitelabel (Phase 3, `900140:244` "추후") — a reader treating all four channels' authenticated/membership session models as MVP would be wrong.
9.7 — Runtime Risk Register
CH-F01 — 0081 hard-defers `order_sessions.customer_id`; 0116 (later) uses it anyway; plus phantom columns across the customer-app RPCs. `DOCUMENT–SQL DRIFT` / `RUNTIME BLOCKER CANDIDATE` · CRITICAL

* Evidence — the deferral gate: `0081` header (concat L9-27) states `order_sessions` "has no customer_id column… Do not re-enable until order_sessions↔customers linkage is explicitly designed (new forward migration required … design lock, human approval, implementation, verification, audit)"; and physically strips `customer_id` from the `order_sessions`/`orders` INSERTs in `place_takeout_order` (concat L817-843) and comments out `customer_order_history`.
* Evidence — the bypass: the newer `0116` functions `bootstrap_customer_app_v2`, redefined `get_customer_home`, `qr_scan_action`, `get_order_tracking`, `get_customer_history` all actively query `os.customer_id = p_customer_id` (concat L3334, L3374, L3798, L3858, L3878, L3966, L3985) — i.e. 0116 re-enabled the exact linkage 0081 forbade, without the required designed-migration gate at that point. The column is only added later by `0148_add_order_sessions_customer_id…` (found in the waiting-order slice), which post-dates 0116.
* Evidence — phantom `pre_order_amount`: `get_customer_home` (0116) reads `os.pre_order_amount > 0` (concat L3949) — the same phantom column the waiting-order correction campaign (0160/0163) removed; it does not exist on `order_sessions`.
* Evidence — phantom `order_items` columns: `place_takeout_order`/`track_takeout_order` (0081) write/read `unit_price`, `subtotal`, `display_order`, `is_kds_required` (concat L880-895, L1085-1090) whereas the live `order_items` schema (carried in the waiting slice) exposes `unit_price_snapshot`, `item_amount`, `selected_options`, `is_kds_required_snapshot` and requires NOT-NULL `menu_code_snapshot` — the same phantom family as workpacket 600680.
* Runtime impact: any call to these 0116 functions before 0148 applied, or any call to `place_takeout_order`/`track_takeout_order` against the current `order_items` shape, raises `42703 undefined_column` (or `23502` for missing `menu_code_snapshot`). At minimum this is a governance-gate breach (0081's explicit "new forward migration + full pipeline" precondition was bypassed by a sibling migration); at worst a live crash surface.
* Owner decision required: (a) confirm whether 0116's customer-linkage was ever taken through the pipeline 0081 demanded; (b) fold the customer-app phantom-column set into the same correction track as 600680.
* Confidence: HIGH on the doc/SQL drift and governance-gate breach (read directly from concat); MEDIUM on crash-certainty (Fable cannot query live schema — candidate, not observed).

CH-F-runtime cross-refs: the payment→KDS positive contract is consistent and correctly implemented (KDS HOLD until `payment_ledger='APPROVED'`; idempotency `WHERE kds_status='HOLD'`; `release_kds_after_payment` SYSTEM-only) — that is a strength, confirmed 900101/900102/900161/900170. The runtime risks are the drift items above plus 005410's `REFUND_BLOCKED_KITCHEN_STARTED` guard (kitchen-cancel ≠ payment-cancel, `005410:372`) which downstream refund SQL must honor.
9.8 — Owner Decision Queue (business/operational — not auto-decidable)

1. No-show / prepaid-cancel KDS fate — ticket CANCELLED (Logic 900101) or stays HOLD-with-grace (contracts + live SQL)? [CH-F02]
2. Order-confirmed-before-payment — ratify 005027 (post-gate) or 005060 (pre-payment)? SQL currently follows 005060. [CH-F03]
3. Provider cutline — ratify Toss+OKPOS (005241) and stamp Toss+PAYCO (005191) superseded; re-point kiosk bundle. [CH-F04]
4. Canonical ChangeContract/TestPlan — declare 900102/900103 canonical, migrate unique 9060xx scope content, retire 906000/906010. [CH-F05]
5. Canonical customer-session table(s) — `order_sessions` vs `customer_app_sessions` relationship; and the still-undesigned `customer_id↔order_sessions` linkage mechanism. [CH-F06, CH-F07, CH-F01]
6. Guest-identity operation — is claim ≡ merge ≡ upgrade, or distinct ops? Fix one term + one preserve-list. [CH-F10]
7. TTL policy — set numeric expirations (token/cart/coupon-hold/guest-session/retention). [CH-F09]
8. Delivery pre-paid no-HOLD bypass — sanction or forbid; reconcile with INV-001. [CH-F11]

9.9 — Regular Workpacket Recommendation Queue (candidates only — Owner-gated per §10)

* WP-A (CRITICAL): Customer-app phantom-column + deferral-gate correction — align `place_takeout_order`/`track_takeout_order`/0116 functions to real `order_items`/`order_sessions` columns and confirm the `customer_id` linkage went through the pipeline 0081 required. Natural sibling of 600680. [CH-F01]
* WP-B (HIGH): No-show KDS-fate reconciliation (Logic vs contracts vs SQL) — decision-first, then doc+possibly-SQL alignment. [CH-F02]
* WP-C (HIGH): Payment-sequencing authority unification (005027 vs 005060). [CH-F03]
* WP-D (HIGH): Provider-cutline supersession + kiosk-bundle re-pointing. [CH-F04]
* WP-E (HIGH): Patent-contract de-duplication (900102/900103 canonical; absorb 9060xx uniques). [CH-F05]
* WP-F (HIGH): Customer-session Source-of-Truth definition + `customer_id↔order_sessions` linkage design (the long-deferred gap). [CH-F06/F07]
* WP-G (MEDIUM): 005012–005020 reference-integrity sweep (−10 offset, 06xxx ids, 005020 paths) + mandatory Status/Supersedes front-matter for policy docs. [CH-F08/F12]
* WP-H (MEDIUM): TTL/expiration numeric specification pass. [CH-F09]

§6.4 baseline for the next slices (waiting / payment / KDS-DID / runtime / app)
Per your request, the contract this policy layer actually imposes, to test the downstream slices against:

1. Payment→KDS (binding, and largely correctly implemented): KDS ticket created HOLD; `HOLD→COMMITTED` only after `payment_ledger.status='APPROVED'`; `release_kds_after_payment()` SYSTEM/service_role-only (authenticated excluded); idempotency `WHERE kds_status='HOLD'`; forbidden direct `HOLD→COOKING/READY/SERVED` and `COMMITTED→HOLD`. Unresolved: order-confirmed-before-payment (CH-F03) and no-show fate (CH-F02).
2. payment_ledger (여전법, hard): persist only `pg_transaction_id/approved_amount/approved_at/payment_method_type`; never PAN/expiry/CVV/RRN; table-order card payments route to certified POS, not tablets (900170).
3. Customer session: two candidate tables (`order_sessions` waiting-lifecycle vs `customer_app_sessions` app/auth) — next slices must be checked for which they read/write and whether `customer_id` linkage exists (CH-F01/F06/F07).
4. No-show / prepaid-cancel sync (005410): waiting/entry/table/POS/payment/kitchen states must not drift silently; kitchen-cancel ≠ payment-cancel; refund blocked if kitchen may have started (`REFUND_BLOCKED_KITCHEN_STARTED`).
5. Secrets/gating (005151): Toss secrets backend-only, never in logs/AI-prompts/exports; Toss payment/cancel/webhook behavior must be officially verified before implementation.
6. Scope hygiene: treat the 12 vision docs (CH-F14) and Ch3/Ch4-membership as future, not MVP contracts.

Everything else the layer emits is state-enum vocabulary only (order-display, payment-status, coupon-lifecycle, support-case, retention, reservation states) — usable as naming references, not as delivered schema.
(Read-only inspection complete. No repository files were created, modified, moved, or deleted; no SQL executed; no git action taken — consistent with operational-plan §4, §5, §13.)
