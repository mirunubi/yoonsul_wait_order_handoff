Reviewer: Opus 4.8

9.1 — Master Inspection Summary
Slice: `slice_02_payment` — Customer Handoff / payment-confirmation layer.
Files inspected: 50 workpacket Markdown (read in full via 3 parallel read-only passes) + 23 SQL migrations (concat read directly — DDL 0014, RPCs 0027/0037/0038/0098/0103/0104, corrections 0142/0143/0153/0154/0157/0158/0159/0166). JSON: none.
Domain reconstruction (§6.2): the payment layer is a heavily-corrected, still-in-motion stack: ledger DDL (0014 `payment_intents`/`payment_ledger`) → intent/confirm/cancel pipelines (0027/0037/0098) → provider adapters (0038 Toss webhook, 0103 Toss Payments, 0104 Toss POS) → a 7-workpacket correction wave (600510→0153, 600540→0154, 600550→0158, 600560→0159, 600570→0037-edit, 600580 state-machine, 600590/601030→0166) + the no-payment pilot (0143). Reference order: `900170 (여전법) + 900101/900102 (Patent-2 late-binding) → 600500 domain → workpackets → SQL`.
Four carried-forward cross-references — resolved:

* #1 (KDS-payment gate / dualization) → partially resolved; asymmetry persists deliberately. There are three KDS-release paths with different gates: `confirm_payment`→`release_kds_after_payment` (0157, force-commits, capacity informational only); `confirm_payment_from_provider`→`request_kds_release_after_payment`→`bulk_commit_kds_tickets` (0166, capacity-gated); and `release_kds_ticket_no_payment` (0143, bypasses payment, store-opt-in, dormant). 601030 wired only the provider path to the gate; both 0157 and 0166 list `confirm_payment` as a non-goal. The base contract (`HOLD→COMMITTED` only after `payment_ledger='APPROVED'`) holds on both payment paths; the 특허2 capacity gate holds only on the provider path. 600591 §3 explicitly warned this creates "새로운 불일치," but 601030 did not carry the warning forward.
* #2 (order_items phantom) → present. A 0104 Toss-POS function reads `oi.unit_price`/`oi.subtotal`/`oi.item_options`/`display_order` (all phantom vs `unit_price_snapshot`/`item_amount`/`selected_options`; no `display_order`). Extends CH-F01/WAIT-F05 into the payment layer.
* #3 (여전법) → held. `payment_ledger` (0014) stores only `provider_payment_key`/`provider_approval_number`/amounts/`provider_approved_at`/`reconciliation_*` — no PAN/expiry/CVV/RRN. Card numbers appear only hashed in `van_transactions` (not `payment_ledger`); raw provider JSON is FK'd out to `provider_raw_events`.
* #4 (audit/event-domain trap) → does not bite payment. `chk_audit_domain` and `chk_event_domain` do differ (`security` audit-only; `waiting` event-only), but the payment layer uses only `payment`/`kds`/`recovery` — valid in both. (Separately, the `chk_audit_**decision**` constraint is violated — see PAY-F01.)

Finding totals: 11 findings — 1 CRITICAL, 4 HIGH, 4 MEDIUM, 2 LOW (+ 1 verified-clean positive).
Confidence & limits: MD/SQL HIGH; crash items are CANDIDATES (Fable cannot execute). New this slice: the workpackets themselves twice caught fabricated "triple-verification" claims — a reason to treat any "삼중검증 완료" framing (including in task inputs) with independent verification.
9.2 — Structural Defect Register
PAY-F04 — Both payment indexes are incomplete (Readme 1/8, NavigationMap 6/8). `STRUCTURAL DEFECT` · HIGH
`600500_Readme` Subfolder Map lists only 600510 (7 of 8 workpackets missing); `600502_NavigationMap` lists 600510/540/550/560/570/580 but omits 600590 and 601030 — and 601030 is the workpacket that shipped 0166 (git-committed). Same class as WAIT-F02: the domain's navigation does not reflect shipped state. Resolution: index-sync workpacket (+ document 0143). Confidence: HIGH.
PAY-F09 — Metadata inconsistency across the wave. `STRUCTURAL DEFECT` · MEDIUM
600510 uses non-conformant bare filenames (`600511_Overview.md`) vs the `000701 §42` number+type+title rule its siblings follow; `600553_TestPlan`/`600554_ChangeContract` have no Status/Stage/Owner header block; Stage taxonomy drifts (ChangeContract `Stage: 2` in 600514 vs `Stage: 3` in 600544; 601030 mixes Stage 1.5/5/11/12 in one file); H1 `.md`-suffix inconsistency; 600510's whole audit trail was retroactively documented (implementation preceded docs); number-space exhaustion forced 601030 to borrow the `601030` block, with a pending `600920` renumbering. Confidence: HIGH.
9.3 — Design Conflict Register
PAY-F02 — KDS-release gate asymmetry persists and is under-acknowledged; the payment→KDS contract has an undocumented sanctioned exception. `DESIGN CONTRADICTION` · HIGH
Three release paths, three gate semantics (staff force-commit / provider capacity-gated / 0143 no-payment). 600591 §1.4/§3 sharply articulated the asymmetry ("완전히 같은 '결제 승인' 이벤트인데도 경로에 따라 다른 커밋 규칙 … 새로운 불일치"), but 601030 executed exactly that option (direction A, provider-only) and filed the residual merely as Open Item (b) "wire POS later" — never justified as an intentional staff-override policy, never re-surfaced as a capacity-rule split. Separately, 0143 (`payment_required_for_kds_release=false` + `release_kds_ticket_no_payment`, requires `can_override_kds`) is a store-scoped opt-out of the entire payment→KDS coupling (the `mvp1_kds_payment_coupling_blocker` bypass) — dormant by default and undocumented in either payment index. Owner decision required: (a) ratify or close the staff-path force-commit; (b) document/govern the 0143 exception. Confidence: HIGH.
PAY-F03 — Two parallel, never-reconciled payment-confirmation pipelines. `CANONICAL AMBIGUITY` / `DESIGN CONTRADICTION` · HIGH
`confirm_payment()` (0098; OKPOS/Toss-POS/Toss-Payments) and `confirm_payment_from_provider()` (0027; Toss webhook/VAN) both confirm payments with divergent behavior and were explicitly left unreconciled (600557 Open Item (a): "remain parallel payment-confirmation pipelines … it does not unify both pipelines"). This is the root of the PAY-F02 gate asymmetry and the deferred customer-facing questions. Owner decision required: designate one canonical confirm path (or a shared core). Confidence: HIGH.
PAY-F10 — Patent package documents no "capacity" mechanism. `MISSING DESIGN CONTRACT` · LOW
600591 §6(e): `grep -rln 'capacity' docs/900000_patent…` → 0. The payment→capacity→KDS 3-step flow that the SQL enforces (provider path) exists in no design-authority doc — the capacity gate is a downstream invention (echoing slice_04 CH-F11). Confidence: HIGH.
9.4 — Canonical Ambiguity Register
PAY-F05 — Governance/provenance integrity: approval-vs-implementation ordering unverified; fabricated verification claims caught twice. `CANONICAL AMBIGUITY` (which state is authoritative) · HIGH
601030 files are header `Status: Draft` while the ChangeContract body declares §10 APPROVED / §11 ACCEPT / §12 READY_FOR_HUMAN_MERGE, and 601032 Logic is frozen at "design-only, not yet live" while 0166 actually shipped and was git-committed (`9565cfe4`); §9 self-approval boxes render ☑ though the doc forbids self-approval, and Open Item 9 admits the commit-vs-approval order is unverified. Two verification-integrity incidents were caught and recorded: a background input falsely claimed Cursor/Codex triple-verification that never happened (600516 §0), and a "Codex" report was a near-verbatim copy of Claude Code's own and was rejected (600546 §0). Impact: for these workpackets, which recorded state is authoritative cannot be read off the docs; "삼중검증 완료" framing is demonstrably unreliable. Owner decision required: reconcile 601030's status/stage and confirm the approval→commit order. Confidence: HIGH.
(The `confirm_payment` vs `confirm_payment_from_provider` canonical question is PAY-F03.)
9.5 — MD–SQL–JSON Drift Register
PAY-F06 — Phantom-column pattern recurs layer-wide; a repo audit is outstanding. `DOCUMENT–SQL DRIFT` · MEDIUM
Confirmed phantom instances in the payment layer: `confirm_payment` INSERT into non-existent `payment_ledger.payment_method`/`provider_tx_id`/`fee_amount`/`provider_response` (+ `tax_amount` in 0130) — fixed by 0158; `orders.paid_at` newly found while fixing `confirm_payment`, replaced locally, but "repo-wide impact … not audited" (600557 Open Item (j)); `order_items` `unit_price`/`subtotal`/`item_options`/`display_order` in the 0104 Toss-POS builder (cross-ref [#2](https://github.com/mirunubi/yoonsul_wait_order_handoff/issues/2)). Root cause (600551 §4): `payment_ledger` deliberately has no generic `updated_at`/`paid_at`, using purpose-specific timestamps. Recommend: the outstanding repo-wide phantom-column scan (spanning WAIT-F05, CH-F01, and this) be run as one audit. Confidence: HIGH.
PAY-F08 — 0159 UNIQUE(`idempotency_key`) applied without auditing other intent-creation key formats. `IMPLEMENTATION DEFECT CANDIDATE` · MEDIUM
The race fix added `uq_payment_intents_idempotency_key` + `ON CONFLICT DO UPDATE`, but whether `0027`/`0142` intent-creation paths can ever emit a colliding non-`OBS-…` key was left unaudited (600562 Open Item (b)) — a live UNIQUE constraint that could reject legitimate inserts from an un-checked path. Confidence: HIGH (gap is documented); MEDIUM (whether it actually collides).
9.6 — Historical / Superseded Candidate Register
PAY-F-hist — 600590 is a stranded/superseded workpacket. `HISTORICAL` · LOW (folded into 9.2/9.3)
600590 ends at Logic with directions A/B "Human-undecided," while 601030 already implemented direction A — 600590 was never marked superseded and still reads as awaiting decision. (Recorded here; remediation is the index-sync of PAY-F04.)
The dropped legacy overloads (0153: 9-param `confirm_payment_from_provider`; 0154: 6-param `mark_payment_uncertain`) are correctly retired — both were independently non-functional against live schema (phantom `payment_intents.provider_payment_key`; invalid `intent_status='UNCERTAIN'` / missing `exception_code`), so no working behavior was removed. Canonical function identity is clean in the live DB; as in the waiting slice, the `0063` source bodies remain in source (WAIT-F04-class residue).
9.7 — Runtime Risk Register
PAY-F01 — Live refund pipeline + Toss-POS builder + audit-decision literals crash against real schema. `RUNTIME BLOCKER CANDIDATE` · CRITICAL
Three confirmed live-crash surfaces, all deferred/unfixed:

1. Refund pipeline (0098 `request_refund`/`confirm_refund`): references `provider_tx_id`/`provider_response`/`refunded_at`/`original_ledger_id`/`refund_reason`/`is_partial_refund`/`fee_amount`/`payment_method` (phantom on `payment_ledger`) and writes `ledger_status='REFUND_PENDING'`/`'REFUND_FAILED'` — absent from `chk_ledger_status`. 600570 confirmed all of these but scoped them out (deferred to a "Refund Pipeline Contract Redesign"), so they are still live. Any refund call raises `42703`/`23514`.
2. Toss-POS order builder (0104): reads `oi.unit_price`/`oi.subtotal`/`oi.item_options`/`display_order` (phantom `order_items`) → `42703` when it runs.
3. Audit-decision literals (601034 §8(h)): seven live files — including 0098 — pass `p_decision` values like `'REFUND_PENDING'` not in `chk_audit_decision`'s allowed set → `append_audit_record` raises `23514` on those branches. Flagged as candidate `audit_decision_literal_repair`, untouched.
Runtime impact: the entire refund path and the Toss-POS push are broken against current schema; audit writes crash on several failure branches. Mitigant: live tables largely 0-row (pre-operation). Recommend: the deferred "Refund Pipeline Contract Redesign" + audit-decision-literal repair + Toss-POS phantom fix be prioritized. Confidence: HIGH (schema mismatch); MEDIUM (crash certainty, un-executed).

PAY-F07 — Test pollution reached the production schema; cross-verifier helpers collided on the shared live DB. `RUNTIME BLOCKER CANDIDATE` / data-integrity · MEDIUM
600560 root-caused a live duplicate `payment_intents` pair (61 ms apart) from a TOCTOU race, and found prior race-testing had left a live `pg_sleep(1.5)` inside `resolve_or_create_payment_intent()` plus a `catchmenu_payment.__pay_con002_slow_resolve()` helper in the real schema (removed before 0159); a separate incident had two verifiers deleting each other's helpers on the shared live DB. Impact: verification was run against a mutable shared production schema without isolation — a governance risk beyond the (now-fixed) specific artifacts. Recommend: enforce the `__test_`-prefix / per-verifier isolation the workpacket proposes. Confidence: HIGH.
9.8 — Owner Decision Queue9.8 — Owner Decision Queue

1. Unify or ratify the payment-confirmation pipelines (`confirm_payment` 0098 vs `confirm_payment_from_provider` 0027) and their KDS-release gates — decide whether the staff path should also be capacity-gated. [PAY-F02/F03]
2. Govern the 0143 no-payment exception — document it in the payment index; decide its pilot scope/authority. [PAY-F02]
3. Refund pipeline — approve the deferred "Refund Pipeline Contract Redesign" (phantom columns + `REFUND_PENDING`/`REFUND_FAILED` + audit-decision literals). [PAY-F01]
4. Repo-wide phantom-column audit — one pass covering `orders.paid_at`, `order_items` (0104/0727), and the customer-app laggards (WAIT-F05/CH-F01). [PAY-F06]
5. 601030 provenance — reconcile Status/Stage and confirm approval-before-commit; adopt a verification-integrity control given the two fabricated-claim incidents. [PAY-F05]
6. Index integrity + renumbering — sync 600500 Readme/NavigationMap (add 540–601030) and resolve the pending `600920` renumbering. [PAY-F04/F09]
7. 0159 UNIQUE-key audit — confirm no other intent-creation path emits a colliding key. [PAY-F08]

9.9 — Regular Workpacket Recommendation Queue (candidates — Owner-gated per §10)

* WP-1 (CRITICAL): Refund Pipeline Contract Redesign — fix 0098 `request_refund`/`confirm_refund` phantom columns + `REFUND_PENDING/REFUND_FAILED` statuses + the `audit_decision` literal repair (7 files). [PAY-F01]
* WP-2 (CRITICAL/HIGH): Toss-POS (0104) phantom `order_items` fix. [PAY-F01/F06]
* WP-3 (HIGH): Payment-confirmation pipeline unification (or shared-core) + staff-path KDS capacity-gate decision. [PAY-F02/F03]
* WP-4 (HIGH): Document + govern the 0143 no-payment exception; index-sync 600500. [PAY-F02/F04]
* WP-5 (MEDIUM): Repo-wide phantom-column audit (`orders.paid_at`, order_items, customer-app). [PAY-F06]
* WP-6 (MEDIUM): Verification-integrity + live-DB test-isolation controls. [PAY-F05/F07]
* WP-7 (MEDIUM): 0159 idempotency-key cross-path collision audit. [PAY-F08]

§6.4 baseline update (payment layer — extending the running dossier)

* payment→KDS contract (refined): `HOLD→COMMITTED` only after `payment_ledger='APPROVED'` holds on both payment paths, but the 특허2 capacity/7-condition gate holds only on the provider path (`bulk_commit_kds_tickets`); the staff path (`release_kds_after_payment`) force-commits. A sanctioned exception (0143 `payment_required_for_kds_release=false`) allows no-payment release entirely. So downstream KDS/DID slices must treat the payment→KDS coupling as three-pathed and non-absolute, not a single invariant.
* 여전법 (confirmed schema-level): `payment_ledger` stores only PG-result fields — no card data. Card numbers only hashed in `van_transactions`; raw provider JSON in `provider_raw_events`.
* Payment ledger real columns: `provider_payment_key`/`provider_approval_number`/`approved_amount`/`cancelled_amount`/`refunded_amount`/`net_amount`/`reconciliation_status`/`kds_release_authorized`. Phantom red-flags for later slices: `provider_tx_id`, `provider_response`(vs `_id`), `refunded_at`, `original_ledger_id`, `paid_at`, `updated_at`, `fee_amount`, `tax_amount`; `ledger_status ∉ {REFUND_PENDING, REFUND_FAILED}`.
* Constraint traps: `chk_audit_decision` is a live crash source (invalid `p_decision` literals); `chk_ledger_status`/`chk_intent_status` reject the phantom statuses; `chk_audit_domain` vs `chk_event_domain` differ but payment uses safe domains.
* Canonical caution: two confirm pipelines (0098 vs 0027) unreconciled; `confirm_payment` deliberately excluded from the 0157/0166 gate work — trace both when KDS/DID slices follow payment callers.

(Read-only inspection complete — no repository files created, modified, moved, or deleted; no SQL executed; no git action, per operational-plan §4/§5/§13.)