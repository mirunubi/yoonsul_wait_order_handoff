Reviewer: Opus 4.8 (Fable 5 세션 도중 자동전환됨)

9.1 — Master Inspection Summary
Slice: slice_01_waiting — Customer Handoff / waiting-order layer.
Files inspected: 65 Markdown (read in full via 6 parallel read-only passes across this and the prior slice) + 15 SQL migrations (schemas 0012/0013, RPCs 0025/0026/0049/0050/0051/0115, corrections 0148/0149/0160/0161/0163/0164/0167 — read directly). JSON: none.
Payload note: input package flags ~1.85 MiB "BORDERLINE/OVER"; handled by fan-out, not sub-split.
Domain reconstruction (§6.2): the waiting layer is a mature, actively-corrected stack: base schema (0012 order_sessions, 0013 orders/order_items) → session/order/settings RPCs (0025/0026/0049) → waiting pipeline (0050/0051/0115) → a 5-workpacket phantom-column correction campaign (600640→0160, 600650→0163, 600660→0164, 600670→0167, 600680→pending) + no-show redesign (600630→0161) + takeout fix (600610) + contract reconciliation (600620) + guest-customer-id linkage (0148/0149 + Flutter 600210). Reference order: 900101/900102 patent authority + 005410 POS-sync policy → 600600 domain → workpackets → SQL → Flutter.
Three carried-forward cross-references — resolved:
CH-F01 → resolved with nuance. order_items (0013) uses unit_price_snapshot/item_amount/selected_options/menu_code_snapshot(NOT NULL)/is_kds_required_snapshot; the customer-app RPCs' unit_price/subtotal/display_order are phantom here too. The phantom pattern is native to this slice (8 confirmed order_sessions phantom columns) but has been systematically fixed by the campaign — except pre_order_while_waiting (still broken, 600680 pending). The customer_id↔order_sessions linkage slice_04 called "never mechanized" is mechanized here (0148 column + 0149 RPC + 600210 Flutter); the customer-app 0081/0116 functions are the un-migrated laggards.
CH-F02 → resolved-in-implementation. SQL (0161 apply_no_show_transition) keeps tickets HOLD with NO_SHOW_GRACE then a batch cancels after expiry — a third path, neither patent 900101's HOLD→CANCELLED nor the contracts' indefinite HOLD. The design (600631 §2 / 600632 §2.2) explicitly documented the patent-package's own internal contradiction (900101 vs 900102/900103/906000/906010) and Human-decided the third path. What remains is that 900101 was never updated — a documentation drift, not an open fork.
CH-F06 → confirmed. Waiting uses catchmenu_pos.order_sessions exclusively (11-state waiting enum); customer_app_sessions has zero references in the entire 600200/600600 tree. The two-table split (waiting-ledger vs app-auth) is real; order_sessions is the waiting Source of Truth.
Finding totals: 14 findings — 1 CRITICAL, 4 HIGH, 5 MEDIUM, 3 LOW/FUTURE + 1 verified-clean (false alarm cleared).
Confidence & limits: MD/SQL cross-checks HIGH (direct file:line). Runtime-crash items are CANDIDATES (Fable cannot execute SQL). Root limitation unchanged from slice_04: no reliable ratified Status convention — this slice adds a fourth Stage-marker scheme.
9.2 — Structural Defect Register
WAIT-F02 — Both domain index files under-list the correction campaign, including an already-committed workpacket. `STRUCTURAL DEFECT` · HIGH
`600600_Readme` Subfolder Map (L23-28) lists only 600610 + 600620 — missing 600630/640/650/660/670/680 (six workpackets, several ACCEPT/APPROVED). `600602_NavigationMap` (L11-16) lists 600610–600660 but omits 600670 and 600680. 600670 is already committed (git `3cbe51b9 …(600670)`) yet appears in neither index; 600680 exists with 4 docs (600681–600684). This violates the project's own "update NavigationMap + index after creating docs" memory rule and means the domain's navigation cannot be trusted to reflect shipped state. Resolution: index-sync workpacket. Confidence: HIGH.
WAIT-F06 — Four Stage-marker conventions + ChangeContract status drift. `STRUCTURAL DEFECT` · MEDIUM
Across five sibling workpackets the same design phase is stamped four ways: 600640 `Stage: 1.5`; 600650/660 `Stage: 2`; 600670 `Stage: 1.5 (Claude Code role)`; 600680 `Stage: 4`. Separately, Flutter ChangeContracts 600214/600224 headers read "Draft / Stage 2 / requires Stage-3 approval" while their §5 shows ☑Approved (2026-07-11) and 600217 records Stage-6 ACCEPT — header status never advanced. Confidence: HIGH.
WAIT-F10 — Stale parent Readme + doc filename churn. `STRUCTURAL DEFECT` · MEDIUM
`600200_Readme` (Draft, 2026-07-11) never registers the existing `600220_platform_deployment_strategy/` subfolder or the Audited 600210 deliverables (children reached Stage 6 on 2026-07-12/13). `600632 §0` records a prior untitled `600632_Logic.md` superseded by a renamed rewrite — two files may coexist on disk (unverified). Confidence: HIGH (Readme staleness); MEDIUM (600632 coexistence).
WAIT-F12 — H1 = filename-with-`.md` on older docs. `STRUCTURAL DEFECT` · LOW
600611/612/614/617/621/622/624/627/634/647/681 use bare `# 600611_Overview.md`-style H1 (literal filename incl. extension) rather than a descriptive title. Cosmetic; not a mismatch. Confidence: HIGH.
9.3 — Design Conflict Register
WAIT-F03 — No-show KDS fate: patent Logic 900101 (`HOLD→CANCELLED`) is stale vs the implemented third path. `DESIGN CONTRADICTION` / `DOCUMENT–SQL DRIFT` · HIGH
Definitive from SQL: `apply_no_show_transition` (0161 L188-198) sets `hold_reason='NO_SHOW_GRACE'` on HOLD tickets and keeps `kds_status='HOLD'`; `expire_no_show_kds_hold` (L387-391) cancels only after `hold_expires_at`; `recover_no_show_grace_ticket` restores HOLD. The design self-identified the patent-package contradiction (600631 §2 L95: 900101 CANCELLED = 1 doc vs 900102/900103/906000/906010 HOLD-kept = 4 docs) and Human-decided grace-then-cancel (600632 §2.2 L89, "재논의 금지"; 600634 APPROVED; 600637 ACCEPT). Nuance vs cancel path: `cancel_waiting` does `HOLD→CANCELLED` (600662 §D.2) — matching Logic for cancellation; the divergence is no-show only. Owner decision required: update 900101 (+ 005410 alignment) to the ratified grace model; the implementation decision is already made. Confidence: HIGH.
WAIT-F07 — "600620 customer_handoff_contract_reconciliation" does not cover customer identity/guest linkage. `DESIGN CONTRADICTION` (name vs scope) / `MISSING DESIGN CONTRACT` · MEDIUM
Despite the name, 600620 reconciles only the RPC-code ↔ table-schema contract (phantom columns) across the Waiting/Order-Session and KDS boundaries (600621 L17-18); `customer_id`/`phone_hash` appear only as passive schema-inventory rows (600621 §1 L65-68), with no design assertion about guest→customer linkage or Source-of-Truth. The actual identity linkage lives elsewhere (0148/0149/600210). Impact: a reader expecting "customer handoff reconciliation" to cover identity finds a title/scope mismatch; the identity contract is fragmented across workpackets. Also unreconciled inside 600620: `cancel_reason`/`no_show_at` classified Correction vs Redesign (600624 §0 L20, left open). Confidence: HIGH.
WAIT-F08 — Design→implementation drifts caught at audit (audit domain; grace-minutes column). `IMPLEMENTATION DEFECT CANDIDATE` · MEDIUM
(1) 600632 §8.1 design writes `p_audit_domain:='waiting'`, but `chk_audit_domain` (on `audit_records`) disallows `waiting`; impl used `'session'` (600637 §3.2). Subtlety: `chk_event_domain` on `ledger.events` does allow `waiting` (added by 0150) — two different constraints with different allowed sets, a live trap. (2) 600632 §11 specifies `store_settings.no_show_kds_grace_minutes` column; impl never added it, using an `information_schema` existence-check with hardcoded 15-min fallback (600637 §3.3) — the "no hardcoding" principle only partially realized. Confidence: HIGH.
WAIT-F09 — `get_waiting_status` RPC exists but the customer status screen calls no RPC. `DESIGN CONTRADICTION` (client incompleteness) · MEDIUM
`waiting_status_screen.dart` makes no RPC call — it reads SharedPreferences and renders stored ids, explicitly declining to invoke a status-lookup RPC (agent-confirmed). Meanwhile SQL `get_waiting_status` (0115/0164) is a full status RPC. So a canonical server capability is unused on the customer path; live wait-status is not fetched. Confidence: HIGH.
9.4 — Canonical Ambiguity Register
WAIT-F04 — Source-file vs migration-history divergence: `0115` still holds the phantom function bodies. `CANONICAL AMBIGUITY` / `HISTORICAL` · HIGH
The entire campaign uses `CREATE OR REPLACE` and (per 0163:29-30 non-goals) never edits `0115` source text. So `0115` source still contains the old phantom `call_waiting_customer`, `seat_waiting_customer`, the 4 siblings, and `pre_order_while_waiting`; the live functions are correct only because 0160/0163/0164 redefined them. 600647 §7.1 flags this stale-source risk (raised independently by Cursor/Antigravity/Claude Code). Impact: anyone reading the migration source for these functions gets the broken version; only live `pg_get_functiondef` (or the latest migration) is authoritative — a systemic canonical-ambiguity across the campaign. Owner decision required: policy on whether superseded source bodies get annotated/superseded-stamped. Confidence: HIGH.
Also latent here: `call_next_waiting` (0050) was DROPped and split into `call_next_waiting_customer` + `_record_waiting_call` (600642 §2.4), while `call_waiting_customer` (0115, different-named duplicate) was kept — 600641 §7 explicitly distinguishes this from a Postgres overload; and the legacy `mark_no_show` overload (0050) was dropped by 0161. Canonical function identity is now clean in the live DB but not in source.
9.5 — MD–SQL–JSON Drift Register
WAIT-F05 — Customer-app RPCs (0081/0116) are the un-migrated phantom laggards; 0116 uses `os.customer_id` before 0148 created it. `DOCUMENT–SQL DRIFT` / `RUNTIME BLOCKER CANDIDATE` · HIGH (continues slice_04 CH-F01)
Against this slice's real 0013/0012 schema: `place_takeout_order`/`track_takeout_order` (0081) and `get_customer_home`/`qr_scan_action`/etc. (0116) reference `unit_price`/`subtotal`/`display_order` (phantom on `order_items`) and `os.pre_order_amount` (phantom on `order_sessions`). The waiting campaign fixed these columns for the waiting functions but not for the customer-app functions. Governance breach: 0081 hard-deferred `order_sessions.customer_id` "until a new forward migration"; 0148 is that migration — but 0116 uses `os.customer_id` and predates 0148 (0116 < 0148), so the usage preceded the column's creation. Additionally 0148 itself records that `customer_id` was first applied out-of-band (no migration, wrong `ON DELETE NO ACTION`) then repaired to `SET NULL`, and `phone_hash` was added because "0115 already assumes it exists" — two more ungoverned/forward-reference drifts native to this domain. Recommend: fold the customer-app functions into the same correction track. Confidence: HIGH (drift); MEDIUM (crash-certainty).
WAIT-F14 — `place_takeout_order` `orders.requested_pickup_at` — audit-time-missing, possibly since added. `DOCUMENT–SQL DRIFT` · LOW
600617 Audit (2026-07-13) recorded `orders` had no `requested_pickup_at`, blocking the INSERT end-to-end. The current `orders` schema dump (carried in slice_04) does list `requested_pickup_at` — so it was likely added by a later migration. Time-dependent; flag for confirm-and-close. Confidence: MEDIUM.
Verified-clean (false alarm cleared): 600212 §9 left "does `chk_event_caused_by_type` accept `p_source='CUSTOMER'`?" unresolved and the Flutter client hardcodes `'CUSTOMER'`. Direct check: 0006:81 allows `CUSTOMER` → no runtime failure. Recorded so the open doc-question can be closed.
9.6 — Historical / Superseded Candidate Register
WAIT-F11 — Self-correcting docs + unresolved line-number dispute (reader-trap). `HISTORICAL` / `STRUCTURAL` · LOW
600671/600672 carry "1차 정정 / 2차 정정" blocks (600671 L131-133) walking back an earlier wrong claim ("0163 established REVOKE+GRANT"; corrected to 0163=REVOKE-only, precedent = 0050:714-719). 600671 Snapshot also records an unresolved `0160:298` vs `0160:297` citation dispute (author re-verified 298, declined the change). Final text is correct but the struck-through middle can mislead. Confidence: HIGH.
WAIT-F13 — Kiosk-platform patent conflict 900161 (Flutter Web) vs 900171 (Android/Windows) surfaced but deferred. `OUT-OF-SCOPE OR FUTURE` · LOW
600221 §0 (L30-31,42) documents an unresolved conflict between two patent docs on the kiosk runtime platform; deferred, not in this change. Belongs to the patent/future domain (slice_04 CH-F14 cluster). Confidence: HIGH.
(`pre_order_while_waiting` phantom body and the un-migrated `0115` source are the concrete superseded-candidate artifacts — see WAIT-F01/F04.)
9.7 — Runtime Risk Register
WAIT-F01 — `pre_order_while_waiting()` is still live-broken (phantom columns + invalid `order_type='TABLE'`); fix designed (600680) but not shipped. `RUNTIME BLOCKER CANDIDATE` · CRITICAL (continues slice_04)
This is the one genuinely still-broken runtime surface in the slice. The live function writes `orders.order_source` (no such column → `42703`), `order_type='TABLE'` (violates `chk_order_type` → `23514`), `order_items.unit_price/subtotal/item_options` (phantom), and `order_sessions.pre_order_amount` (phantom); it also never sets `order_sessions.order_id`/`pre_order_created_at`, so the corrected siblings' `has_pre_order` derivation returns false. It is the "fifth sibling" the campaign explicitly forecast (600651 §4.2 / 600652 §8(b) / 600662 §D.1); workpacket 600680 (Overview/Logic/TestPlan/ChangeContract) designs the correction and awaits Human approval + Stage-8 implementation. Until 0168+ ships, any call crashes. Mitigant: no live callers observed (0-row evidence). Recommend: prioritize 600680 through the pipeline. Confidence: HIGH (broken-against-current-schema); MEDIUM (crash certainty, un-executed).
Cross-refs: WAIT-F05 (customer-app phantom laggards) and WAIT-F08 (`audit_domain='waiting'` / grace-minutes) are the other runtime-adjacent items. The positive runtime contract — KDS `HOLD` until `payment_ledger='APPROVED'`, no-show grace, idempotency `WHERE kds_status='HOLD'` — is correctly and consistently implemented (0161/0160/0163/0164), a slice strength.
9.8 — Owner Decision Queue

1. Ratify the no-show KDS model and update patent Logic 900101 (+005410) to the implemented `HOLD→NO_SHOW_GRACE→auto-CANCELLED` third path. [WAIT-F03 / CH-F02] — implementation already decided; docs must catch up.
2. Customer-app correction scope — bring 0081/0116 phantom columns + the pre-0148 `os.customer_id` usage into a correction workpacket (sibling of 600680); confirm the out-of-band `customer_id`/`phone_hash` history is fully governed. [WAIT-F05 / CH-F01]
3. Source-vs-migration policy — decide whether superseded function bodies in `0115` (and peers) get annotated/superseded-stamped to remove the canonical ambiguity. [WAIT-F04]
4. Index/navigation integrity — mandate NavigationMap + Readme sync (600670 is committed but unindexed). [WAIT-F02]
5. Stage-marker convention — pick one lifecycle-stage scheme across workpackets. [WAIT-F06]
6. Customer wait-status client path — decide whether `get_waiting_status` should be wired into `waiting_status_screen.dart`. [WAIT-F09]

9.9 — Regular Workpacket Recommendation Queue (candidates — Owner-gated per §10)

* WP-1 (CRITICAL): Ship 600680 (`pre_order_while_waiting` phantom correction) — already designed/contracted; needs approval → Stage-8. [WAIT-F01]
* WP-2 (HIGH): Customer-app RPC phantom + `customer_id` correction (0081/0116), sibling of 600680. [WAIT-F05]
* WP-3 (HIGH): Patent-doc reconciliation — update 900101 & 005410 to the ratified no-show grace model. [WAIT-F03]
* WP-4 (HIGH): Index/NavigationMap/Readme sync for 600600 + 600200 (add 600630–600680, 600220). [WAIT-F02/F10]
* WP-5 (MEDIUM): Source-of-truth annotation pass for superseded `0115`/`0050` bodies. [WAIT-F04]
* WP-6 (MEDIUM): Stage-marker + ChangeContract-status normalization across the campaign. [WAIT-F06]
* WP-7 (MEDIUM): Wire `get_waiting_status` into the customer status screen (or document why not). [WAIT-F09]
* WP-8 (LOW): Confirm-and-close `orders.requested_pickup_at` (likely already added). [WAIT-F14]

§6.4 baseline update (waiting layer — refining the slice_04 dossier for downstream slices)

* Session Source of Truth = `catchmenu_pos.order_sessions` (11-state waiting enum), guest-linked via `customer_id`(`ON DELETE SET NULL`)/`is_guest`/`phone_hash` (0148) and `get_or_create_guest_customer`/`register_waiting` (0149). `customer_app_sessions` is a separate app-auth table (slice_04) — downstream slices must not conflate them (CH-F06 confirmed).
* `order_items` real columns: `menu_code_snapshot`(NOT NULL)/`unit_price_snapshot`/`item_amount`/`selected_options`/`options_amount`/`*_snapshot`. Any RPC using `unit_price`/`subtotal`/`display_order` is phantom (test 600500 payment / 600400 KDS slices against this).
* No-show KDS contract (ratified in implementation): `HOLD → NO_SHOW_GRACE(+hold_expires_at) → auto-CANCELLED`; cancel path is immediate `HOLD→CANCELLED`. Patent 900101 is stale on this.
* Constraint traps for next slices: `chk_audit_domain` (audit_records) ≠ `chk_event_domain` (ledger.events) — `waiting` is valid only for the latter; `chk_event_caused_by_type` allows `CUSTOMER`.
* Known still-broken: `pre_order_while_waiting` (until 600680 ships) and the customer-app 0081/0116 functions — treat as live-defect zones when KDS/payment slices trace callers.

(Read-only inspection complete — no repository files created, modified, moved, or deleted; no SQL executed; no git action, per operational-plan §4/§5/§13.)