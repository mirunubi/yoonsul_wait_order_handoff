# 600301_ChangeHistory.md

Per `000701` §30 — single running file, append-only. One row per change.

| Date | Change Description | Reason/Evidence | Outcome | Linked Audit/Test |
|---|---|---|---|---|
| 2026-07-11/12 | Full cloud migration sync: sequential replay of `sql/migrations/0000`–`0150` against cloud project `upzthfwhtvazfftxnyfu` via `tools/apply_migrations_cloud.py`, 19 files skipped/accepted after root-cause review (constraint-widening order, forward references), data backfill of 52 `error_codes` / 22 `documents` / 4 `subscription_plans` rows | Cloud had never been synced to the current local migration history (`catchmenu_meta` schema absent, per Q7); cloud also carried 64 pre-existing dev/test menu rows not reflected in local expectations | ACCEPT — cloud/local verified matching on all three backfilled tables (246/22/8 both sides) as of 2026-07-12 | `600311_Overview.md` |
| 2026-07-13 | `scheduled_pull_based_secondary_backup`: created `tools/cloud_target_config.py` (shared project-identity constants) and `tools/pull_secondary_backup.py` (pull-based secondary backup of the cloud project into `E:\catchmenu_backups\`, 30-day retention, dry-run default) | No secondary backup existed outside the primary cloud project itself; PC-triggered pull model chosen over an always-on cloud scheduler for MVP-stage cost/simplicity (`600324_ChangeContract.md` §4, re-confirmed, not reopened) | ACCEPT — `600321`–`600324`(Overview/Logic/TestPlan/ChangeContract)/`600325_Module.md`(Stage 4, includes BOM discovery+fix in `read_linked_project_ref()`)/`600326_Verification.md`(Stage 5, Codex self-check + Human's 4 direct executions + Claude Code independent re-execution of all 4, all PASS)/`600327_Audit.md`(Stage 6) all complete. 4 Open Items carried forward: (a) no real `--execute` cloud dump run yet, pending separate Human approval, (b) `apply_migrations_cloud.py` refactor to import the shared constants module deferred (already-Audited file, §37), (c) Task Scheduler time (03:00)/retention (30 days) still provisional, (d) restore-path verification is a separate future workpacket | `600320_scheduled_pull_based_secondary_backup/600321`–`600327` |
