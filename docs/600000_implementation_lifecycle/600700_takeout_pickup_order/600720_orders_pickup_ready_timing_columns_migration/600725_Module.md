# 600725_Module.md

Status: Implemented
Lifecycle: Module
Stage: 4
Owner: Codex
Date: 2026-07-13

## Summary

Implemented the approved `orders_pickup_ready_timing_columns_migration` change (`600722_Logic.md` §1): a new forward migration `sql/migrations/0152_add_orders_pickup_ready_timing_columns.sql` adding two nullable, no-default columns to `catchmenu_pos.orders`.

```sql
alter table catchmenu_pos.orders
  add column if not exists requested_pickup_at timestamptz,
  add column if not exists ready_at timestamptz;
```

| File | Change | Result |
|---|---|---|
| `sql/migrations/0152_add_orders_pickup_ready_timing_columns.sql` (new) | `ALTER TABLE catchmenu_pos.orders ADD COLUMN requested_pickup_at timestamptz, ADD COLUMN ready_at timestamptz` | Applied, live-verified. |

## Design Match

Matches `600722_Logic.md` §1 exactly: both columns `timestamptz`, nullable, no default, no `CHECK` constraint — same pattern as the pre-existing `confirmed_at`/`cancelled_at`/`completed_at` columns confirmed in `0013_create_pos_orders.sql`. Codex added `if not exists` to both `ADD COLUMN` clauses (defensive idempotency, not part of the original design but does not change behavior — a second application is a no-op instead of an error).

No function body (`0081`/`0079`/`0094`) was modified — matching `600722_Logic.md`'s explicit statement that this migration is schema-only. Depends-on header correctly references `0151` (the immediately preceding migration in sequence).

## Boundary Notes

- Only `catchmenu_pos.orders` was altered — no other table touched.
- No `NOT NULL`, `DEFAULT`, or `CHECK` constraint added, matching the file's own header comment and `600722_Logic.md` Open Question 1/2 resolution.
- `catchmenu_kds.kds_tickets.ready_at` (pre-existing, unrelated column) was not touched.
- PICKED_UP status drift, `point_ledger`, `discount_pct` were not addressed — out of scope, unaffected by a schema-only change.
- Checksum bookkeeping correct — file SHA-256 (CRLF-normalized) matches `catchmenu_meta.migration_history` exactly, `success = true`.
- No cloud database was touched. No git commit was performed.
