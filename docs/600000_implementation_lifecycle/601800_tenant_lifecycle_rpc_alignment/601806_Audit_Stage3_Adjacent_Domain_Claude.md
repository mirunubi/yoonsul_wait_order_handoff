# 601806_Audit_Stage3_Adjacent_Domain_Claude.md

Status: Active
Lifecycle: Audit
Last Updated: 2026-08-28

## §0 수행 조건

`000701` §47.1 이 3단계 actor 를 `Opus/Fable` 로 지정한다.
**이 검증이 그 요건을 충족한다.**

```text
수행        Claude (Opus), 새 대화창
사전 맥락    0 — 이 워크패킷을 그 대화에서 처음 봤다
저장소 접근  없음. 첨부 7건으로만 판단
결과 전달    답변 출력. Human 이 이 파일로 편입
```

**첨부**

```text
601801 · 601802 · 601803
000220 · 000221 · 601702 · 601746
```

> ⚠️ **`isolate_tenant` 문제를 사전 고지하지 않았다.**
> **`601803` 작성 의도도 설명하지 않았다.**

**동시 수행 — 상호 참조 없음**

```text
Verifier A   Codex    601804
Verifier B   Cowork   601805
Verifier C   Claude   이 문서
```

## §1 검증 결과 — VERBATIM

**아래는 답변 원문이다. 편집하지 않았다.**

````text
## 사전 확인
```text
601803  SHA-256  실측 def851be…d87561  = 워크패킷 지정값  일치 ✓
대조 기준 6건 전부 수신 ✓
```
## 종합
| 관점 | 발견 | Blocker | informational |
|---|---|---:|---:|
| C1 V모델 짝짓기 | 2 | 1 | 1 |
| C2 어휘 충돌 | 3 | 2 | 1 |
| C3 관계 · 참조 충돌 | 1 | 1 | 0 |
| C4 경계 침범 | 1 | 0 | 1 |
| C5 선언 대 모델 완결성 | 5 | 2 | 3 |
| C6 모델 자체 정합 | 5 | 1 | 4 |
| **계** | **17** | **7** | **10** |
## Findings
| # | 관점 | 지점 | 내용 | Blocker | rule 근거 |
|---|---|---|---|---|---|
| C-B1 | C6 | `601803` §5 흐름도 노드 `Q` | 판정 분기가 `"tenant 가 ISOLATED 인가"` 단일 축이다. `아니오` 경로가 곧바로 `영업 side effect 적용`으로 간다. 그러나 같은 문서 §3 합성 규칙표는 서비스 접근을 「두 축 중 더 제한적인 조건」으로 정의했고, `601801` `HG-A-9-6` 은 `CANCELLED` 에서 일반 영업 서비스를 재개하지 않는다고 선언했다. `CANCELLED`+`NONE` tenant 의 외부 이벤트가 §5 대로면 side effect 가 적용된다 | **Y** | 1 · 5 |
| C-B2 | C5 | `601803` §1 T-1 근거란 · §2 I-1 근거란 | 두 축의 초기값(`TRIAL` · `NONE`)을 `허용`으로 판정하면서 근거를 `601802` §6.1 의 DB default 로 삼았다. `601801` 에 초기 상태 선언은 없다. `601702` §1.27 은 tenant-level `TRIAL` 값의 존재를 설계 근거로 쓰는 것을 「과거 구현을 설계 근거로 승격시키는 것」이라 명시했고 `601702` §3 이 같은 제약을 반복한다. §7 U-4 는 「경로」만 미정으로 두고 「값」은 확정된 것처럼 남겼다 | **Y** | 2 · 4 |
| C-B3 | C2 | `601803` 전체 — 「격리」 어휘 | `601702` §1.26 은 `Tenant` 를 「Store 의 두 번째 구조 부모가 아니라 **필수 격리 scope**」로 규정한다. 이때 격리는 멀티테넌트 데이터 경계 · RLS 다. `601801` `HG-A-1` 의 `isolation_state` 는 보안 · 장애 대응 격리다. `601803` 은 두 의미를 구분하지 않고 「격리」를 단일 어휘로 쓴다. 4단계가 물리 객체 · policy 를 명명하는 단계라 충돌 지점이다 | **Y** | 4 |
| C-B4 | C1 | `601803` §3 합성 규칙표 「서비스 접근」 행 | 서비스 접근을 `tenant_status` × `isolation_state` 2축으로만 합성한다. `601702` §1.27 은 Store 상태를 **Store Service Status · Store Operating Status · Trial Status** 세 축으로 분리했고, §1.28 은 `TenantStatus ≠ StoreServiceStatus ≠ TrialStatus ≠ IsolationState` 를 열거하며 「상위 상태를 하위 상태의 대체물로 사용하지 않는다」고 못박았다. §3 격자의 `TRIAL` 행 「체험 서비스」 표기도 `601702` §1.27 의 「tenant-level TRIAL 을 Store Trial Status 와 동일시할 근거는 없다」와 맞선다. §7 미정 17건에 이 항목이 없다 | **Y** | 4 |
| C-B5 | C3 | `601803` 전체 — `store` · `merchant_account` 부재 | `601746` §4 후속 이관 목록이 「tenant 상태 ↔ merchant account 계약 상태 미연결 — **상태 축 후속** — §2.11 d」를 이관했다. `601803` 은 상태 축 모델인데 `merchant_account` 를 한 번도 다루지 않고 §7 미정에도 기록하지 않았다. `601702` §1.45 는 Tenant ↔ MerchantAccount 를 1:1 필수로 두었으므로 tenant 격리 시 하위 `store` · `merchant_account` 의 처분은 공백으로 남는다. `HG-A-2` 의 「신규 store · user 생성 차단」은 U-16 으로 자기기록됐으나 **기존** store 의 상태 효과는 어디에도 없다 | **Y** | 4 |
| C-B6 | C2 | `601803` §7 U-14 | `601802` §7.2 는 두 개가 아니라 세 개의 lifecycle 값집합을 실측했다. `tenants.tenant_status`={ACTIVE,TRIAL,SUSPENDED,CANCELLED,TERMINATED} · `tenant_plan_configs.plan_status`={TRIAL,ACTIVE,SUSPENDED,CANCELLED,EXPIRED} · `white_label_configs.contract_status`={NEGOTIATING,SIGNED,ACTIVE,SUSPENDED,TERMINATED}. U-14 는 앞의 둘만 기록했다. 세 집합이 `ACTIVE`·`SUSPENDED`·`CANCELLED`/`TERMINATED` 를 공유하므로 「다른 것을 같게 부르는」 전형이며, `contract_status` 는 C-B5 의 계약 상태 미연결과 같은 대상이다 | **Y** | 4 |
| C-B7 | C5 | `601803` §3 합성 규칙표 「사용량 산정」 행 | `tenant_status` 를 **미관여**로 판정했다. `601801` 은 이 부정 선언을 하지 않았다. `HG-A-4` 는 격리 축만 다루고 `HG-A-9-3` 은 `tenant_status` 에 계약 · 과금을 배정했으며 `601801` §2 는 `CANCELLED` 이후 「반복과금 없음」 · `TERMINATED` 이후 「과금 없음」을 규정한다. 모델이 선언에 없는 부정 판정을 만들어냈다 | **Y** | 2 |
| C-I1 | C6 | `601803` §2 mermaid vs 판정표 I-4 | 같은 규칙을 두 곳이 다르게 그렸다. 다이어그램은 `ISOLATED --> ISOLATED : TERMINATED tenant — 해제 불가` 로 자기전이를 그렸고, 표 I-4 는 전이를 `ISOLATED → NONE` 으로 적고 「금지」로 판정했다. 전자는 전이의 존재를, 후자는 부재를 뜻한다 | N | — |
| C-I2 | C6 | `601803` §8.1 추적표 마지막 행 | `§7 U-1 ~ U-15` 로 적혀 있으나 §7 본문은 U-17 까지이고 「미정 17건」으로 닫는다. U-16 · U-17 이 추적표에서 누락됐다 | N | — |
| C-I3 | C6 | `601803` §4 대조표 | `601802` §5.3 은 `isolate_tenant` 의 DB 직접 호출자를 `detect_threat` · `manage_subscription` 2건으로 실측했다. §4 는 `manage_subscription` 만 승계하고 `detect_threat` 를 다루지 않는다. 자동 위협 탐지가 격리를 발동하는 경로이므로 §7 U-6(격리 발동 권한 주체)과 §6 B-2(귀책 주체 자동 판정)에 직결된다. `601802` §9.1 은 `detect_threat` 의 FATAL 경로에도 `p_reason` 인자명 불일치가 있다고 기록했으나 §0.1 은 불일치를 2곳으로만 옮겼다 | N | — |
| C-I4 | C6 | `601803` §6 mermaid 노드 `J` | 분기 4개가 배타로 그려져 `TRIAL`+`ISOLATED`+플랫폼 귀책이 `T2` 로만 간다. `HG-A-5` 는 플랫폼 귀책 격리에 billing review 를 무조건 생성하라 하고 `HG-A-9-4` 는 trial extension review 를 추가한다. 두 review 의 중첩 여부를 `601801` 이 명시하지 않았는데 다이어그램이 배타로 암묵 판정했다. §7 미기록 | N | — |
| C-I5 | C5 | `601803` §5 | `HG-A-7` 표 7행 중 「KDS release 금지」가 §5 에 개별 표현되지 않았다. `H["quarantine / pending review 보류"]` 에 포함된 것으로 읽을 수는 있으나, `HG-A-2` 의 KDS 항목은 U-16 으로 별도 기록된 것과 비대칭이다. §7 미기록 | N | — |
| C-I6 | C2 | `601803` §5 노드 `E` | `외부 provider 이벤트` 단일 노드다. `601702` §1.43 은 provider 를 **거래 provider**(`TOSS-TX` — 주문·결제·정산)와 **실행 provider**(`SC-EXEC` — KDS·DID)로 나누고 「성격이 다르다」고 선언했다. §5 Q-2 의 「provider 식별」이 이를 흡수하는지 명시되지 않았다 | N | — |
| C-I7 | C4 | `601803` §7 U-7 | 「더 제한적인 조건」의 판정 위치를 `RLS · 함수 · 애플리케이션` 중 미정으로 두면서 소관 나선을 적지 않았다. `000221` §4.4 는 「0-C 에서 **처음으로** RLS policy 를 만든다」고 배정했고 `601702` §1.45 는 0-A 가 0-C 권한 모델을 선결정하지 않도록 못박았다. U-4 가 「provisioning 소관 — 0-A-3 경계와 겹친다」로 소관을 명시한 것과 비대칭이다. **선점은 아니나 4단계가 U-7 을 0-A-2 안에서 닫으면 0-C 침범이 된다** | N | — |
| C-I8 | C1 | `601803` §5 Q-2 · Q-7 | `601802` §8.1 이 `catchmenu_common.idempotency_keys` 를 21컬럼(`provider_event_id` · `replay_count` · `max_replay_allowed` · UNIQUE`(tenant_id,key_domain,idempotency_key)`)으로 실측했다. §5 는 `offline_queue` 는 주석으로 승계했으나 이 기존 자산은 언급하지 않는다. 증거 승계가 비대칭이다 | N | — |
| C-I9 | C5 | `601803` §2 I-2 | 전제 조건을 「격리 사유 발생 — 사유 분류는 `601801` §2」로 참조 처리했다. `601801` §2 마지막에서 두 번째 행은 「고객의 자발적 사용 중지 → **`ISOLATED` 사용 금지**」이며 ⚠️ 로 「격리를 편의상 재사용하면 축이 오염된다」고 강조한다. 이 **금지**가 전이 모델에 표현되지 않았다 | N | — |
| C-I10 | C5 | `601803` §3 10개 조합 격자 | `HG-A-9` 표를 그대로 옮겼으나 `601801` §2 의 격리 원인별 처분 8행 중 `CANCELLED` 이후 「반복과금 없음」 · `TERMINATED` 이후 「과금 없음」이 격자에도 합성 규칙표에도 없다. §8 이 `601801` §2 를 인용 범위로 선언한 것과 어긋난다 | N | — |
## `HG-A-N` 추적표
| `HG-A-N` | `601803` 표현 위치 | 완결 |
|---|---|---|
| `HG-A-1` 상태축 독립성 | §3 격자 · §3 축 다이어그램 | 완결 |
| `HG-A-2` `ACTIVE`+`ISOLATED` 의 의미 | §3 합성 규칙표 「서비스 접근」 행이 차단 범위를 `HG-A-2` 표로 **참조**만 함 | **부분** — 기능별 9행 미표현. §7 U-16 으로 자기기록됨 |
| `HG-A-3` 격리와 상업 상태 분리 | §2 I-2 · §4 책임 경계 다이어그램 | 완결 |
| `HG-A-4` 기본료 · 사용량 과금 | §3 「사용량 산정」 행 · §5 Q-8 · Q-9 | **부분** — 시간축 3분류 미표현. §7 U-17 로 자기기록됨. 단 C-B7 은 별건 |
| `HG-A-5` Human 과금 검토 | §6 전문 · B-1~B-8 | 완결. B-8 의 「장기 격리」 이월은 `HG-A-5` 자체 지시에 부합 |
| `HG-A-6` 구독 변경과 격리 해제 분리 | §1 T-2~T-7 · §4 다이어그램 `F2 -.-> S2` | 완결 |
| `HG-A-7` 외부 이벤트 수신 · side effect 격리 | §5 흐름도 · Q-1~Q-10 | **부분** — 7행 중 「KDS release 금지」 미표현(C-I5). 판정 분기 결함은 C-B1 |
| `HG-A-8` 격리 해제 승인 | §2 I-3 · 3요건 전용 다이어그램 | 완결 |
| `HG-A-9-1` 5×2 표현 가능 | §3 격자 10칸 | 완결 |
| `HG-A-9-2` 더 제한적인 조건 | §3 합성 규칙표 | **부분** — §5 가 이 규칙을 따르지 않음(C-B1). Store 축 누락(C-B4) |
| `HG-A-9-3` 계약은 `tenant_status`+subscription | §1 T-2~T-7 근거란 · §3 합성 규칙표 | 완결 |
| `HG-A-9-4` `TRIAL`+`ISOLATED` | §6 노드 `T2` · `X` | **부분** — 배타 분기 문제(C-I4) |
| `HG-A-9-5` `SUSPENDED` 해제 후에도 정지 유지 | §2 말미 ⚠️ | 완결 |
| `HG-A-9-6` `CANCELLED` | §1 T-9 · §2 말미 ⚠️ | 완결 |
| `HG-A-9-7` `TERMINATED` terminal containment | §2 I-4 · 해제심사 다이어그램 note · §1 T-11 | 완결. 표현 불일치는 C-I1 |
| `HG-A-9-8` 양방향 자동변경 금지 | §1 말미 ⚠️ · §2 말미 ⚠️ · §3 축 다이어그램 · §3 합성표 | 완결 |
**`601801` 에 없는 선언을 모델이 만들어낸 지점 — 2건: C-B2 · C-B7.**
## 확인 불가 항목
저장소 · DB 접근이 없어 판단하지 못한 것이다. 추정으로 채우지 않았다.
```text
601800 §1 · §5 · §6         601803 §8 이 ACTIVE 근거로 인용. 미첨부
000701 §46 · §47.1 전문      워크패킷 인용문 외 원문 미첨부
                            → 2단계 산출물 규격 · 3단계 actor 배정의 전문 확인 불가
600010 §1.1 · §2            Gate A 발생 경위 · 0-A-2 파생 경위. 미첨부
601502 · 601503 · 601505    권위보류 대역이자 미첨부
601510 · 601511             동상
601744 · 601745 · 601747    601746 의 상위 증거. 미첨부
010640 · 010004 · 000170    601702 §1.26 · §1.27 · §1.29 가 인용한 상위 문서. 미첨부
000150 · 000190 · 000718    동상
```
```text
DB 실측 재확인               601802 의 컬럼 · CHECK · 함수 · ACL 값을 그대로 사실로 취급했다
                            독립 재측정을 하지 않았다
mermaid 렌더 결과            텍스트로만 판독했다. 렌더 후 시각적 불일치 여부는 미확인
601803 §4 「관측되지 않았다」  601802 §5 · §9 관측 범위 밖에 복구 함수가 존재하는지 확인 불가
```
**C-B5 의 소관 귀속** — `601746` §4 는 「상태 축 후속」이라고만 적었고 `000221` §3~§4 는 이 항목에 나선 번호를 배정하지 않았다. **0-A-2 소관인지 자체가 확인 불가이며, finding 은 「`601801` · `601803` 어느 쪽에도 기록되지 않았다」는 사실에 한정된다.** 처분은 4단계가 정한다.
````

## §2 Status

```text
Audit evidence only.
```

**처분은 4단계가 정한다. 통합은 `601807` 이 수행한다.**
