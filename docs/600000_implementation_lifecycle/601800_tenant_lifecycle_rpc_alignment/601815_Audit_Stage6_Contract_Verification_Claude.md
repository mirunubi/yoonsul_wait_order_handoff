# 601815_Audit_Stage6_Contract_Verification_Claude.md

Status: Active
Lifecycle: Audit
DocumentType: Audit
Last Updated: 2026-08-29

## §0 수행 조건

`000701` §9.16 Stage 6 · §37 원작자 배제.

```text
수행        Claude (Opus), 새 대화창
사전 맥락    0
저장소 접근  없음. 첨부 9건으로만 판단
결과 전달    답변 출력. Human 이 이 파일로 편입
```

**첨부**

```text
검증 대상   601809 · 601810 · 601811 · 601812
근거        601801 · 601802 · 601803 · 601808 · 601702
```

**동시 수행 — 상호 참조 없음**

```text
Verifier A   Codex    601813   blocking 10
Verifier B   Cowork   601814   blocking  4
Verifier C   Claude   이 문서   blocking 15
```

## §1 검증 결과 — VERBATIM

**아래는 답변 원문이다. 편집하지 않았다.**

첨부 9건만 읽고 판정했다. 저장소의 다른 파일·DB·migration 원문은 열지 않았다.

## 종합

| 축 | 발견 | blocking | informational |
|---|---|---:|---:|
| 1. 외부 타당성 — 선언한 불변조건이 실제로 강제되는가 | 9 | 9 | 3 |
| 2. 상위 선언 위반 | 2 | 2 | 1 |
| 3. 수직 추적성 — 의미 변질 · 누락 | 3 | 2 | 3 |
| 4. 모델 자체의 정합 (범위=불변조건=기대값=허용조작) | 12 | 2 | 4 |
| **계** | **26** | **15** | **11** |

> ⚠️ 축이 겹치는 발견은 주된 축 1곳에만 계상했다.

---

## Findings — blocking

| # | 축 | 지점 | 내용 | blocking | rule 근거 |
|---|---|---|---|---|---|
| **B-1** | 1 | `601812` §2.5 `D-28` · §1 · §4.2 `FO-6`·`FO-7` | `is_tenant_access_allowed` 를 **호출하는 경로가 계약 안에 없다.** 허용 파일은 `A-1` migration 1건뿐이고(§1) 애플리케이션 코드는 허용 파일이 아니며(§7), `FO-6` 이 `CREATE POLICY` 를, `FO-2`·`FO-3`·`FO-4` 가 기존 RPC 수정을 금지한다. 계약대로 구현하면 `isolation_state` 는 쓰이지만 **어떤 runtime 경로도 그 값을 읽지 않는다.** `601812` §7 이 「mutation RPC 가 tenant 상태와 격리를 다시 확인한다」고 적었으나 그 mutation RPC 는 전부 수정 금지 대상이다. `601810` `I-12`·`I-13`·`I-14`(`HG-A-2`·`HG-A-9.2`)가 선언한 차단이 강제되지 않는다. `601811` `TP-P-9`(「일반 runtime 접근이 거부된다」)의 기대값을 만족시킬 주체가 없다 | **YES** | 4 · 1 |
| **B-2** | 1 | `601812` §2.6 · §2.5 `D-26` | `D-26` 의 시그니처는 `p_isolate boolean` 이고(`601802` §5.1) §2.6 이 본문 요건으로 **「`isolation_state` 를 `'ISOLATED'` 또는 `'NONE'` 으로 쓴다」**를 명시했다. 즉 `isolate_tenant(p_isolate := false)` 가 **3요건을 거치지 않는 두 번째 해제 경로**가 된다. `601801` `HG-A-8`·`601810` `I-22`·`I-23`(「세 요건 중 하나라도 미충족이면 `ISOLATED` 가 유지된다」)이 `D-27` 에만 걸려 있고 `D-26` 에는 걸려 있지 않다. `601811` `TP-P-5`·`TP-N-8` 이 이 경로로 FAIL 한다 | **YES** | 2 · 4 · 1 |
| **B-3** | 1 | `601812` §2.1 `D-1` · §2.5 `D-27` · §3 `M-1` | 해제 3요건의 물리 표현은 `D-1` 의 `cause_resolved`·`human_approved`(둘 다 `DEFAULT false`)다. **그 두 값을 참으로 만드는 조작이 계약 어디에도 없다.** `D-27` 의 인자는 `(p_tenant_id, p_event_id, p_actor_id, p_locale)` 로 승인 입력이 없고, `M-1` 「허용 DML 없음」이며, `D-23`·`D-24` FORCE RLS + policy 0 + `FO-7` GRANT 금지로 외부 UPDATE 경로도 없다. 계약대로 구현하면 **`ISOLATED` 는 영구히 해제 불가**이고 `601811` `TP-P-4`(3요건 충족 시 `NONE`)의 positive case 를 만들 수 없다 | **YES** | 1 · 4 |
| **B-4** | 1 | `601812` §2.1 `D-1` · §2.2 `D-6` · §5 · §8.2 | `D-1.idempotency_key` 는 `NOT NULL`, `D-6` 은 `UNIQUE(tenant_id, event_type, idempotency_key)`, §5 는 요청 멱등성 scope 를 그 세 값으로 확정했다. 그런데 §8.2 가 **시그니처 불변(선택지 C)** 을 판정했으므로 `isolate_tenant` 의 인자에 `idempotency_key` 가 없고(`601802` §5.1), `D-27` 에도 없다. **키를 어디서 얻는지가 미확정**이며, 함수 안에서 생성하면 `601810` `I-29`(동일 요청 재실행)가 성립하지 않는다. `601812` §5 ⚠️ 가 `TP-C-1`·`TP-C-2` 의 판정 기준을 `D-6` 으로 지정했으므로 그 두 Test 가 FAIL 하거나 실행 불가다 | **YES** | 1 · 3 |
| **B-5** | 1 | `601812` §2.5 `D-26`~`D-29` · §2.3 `D-23`·`D-24` | 네 함수의 **`SECURITY DEFINER`/`INVOKER` · 소유자 · `SET search_path` · 반환형이 계약에 없다.** `601802` §5.1·§9.2 실측은 `isolate_tenant` 가 `SECURITY DEFINER`, `search_path = common,hq,ledger,audit` 다. `CREATE OR REPLACE` 는 명시하지 않은 속성을 기본값으로 되돌리므로 `D-26` 을 그대로 적으면 **DEFINER 와 search_path 가 소실**된다. `601809` §4.2 근거 3 「수리는 권한 표면을 건드리지 않는다」가 성립하지 않으며 `601811` 에 `prosecdef`·`proconfig` 검사 항목이 없다(`TP-R-10` 은 EXECUTE ACL 만 본다). 또한 `D-23`·`D-24` 가 세 신규 테이블에 FORCE RLS 를 걸고 policy 0 이므로, 실행 권위가 확정되지 않으면 **`tenant_isolation_events` INSERT 자체가 실패**하여 `TP-P-6`·`TP-C-5` 가 FAIL 한다 | **YES** | 3 · 4 · 1 |
| **B-6** | 1 | `601802` §9.2 · `601812` §4.2 `FO-7` · `601811` `TP-R-10` | `isolate_tenant` 의 EXECUTE ACL 은 `postgres, authenticated` 이고, `FO-7` 이 `GRANT`·`REVOKE` 를 금지하며 `TP-R-10` 은 ACL **불변**을 PASS 조건으로 삼는다. 함수는 `p_tenant_id` 를 인자로 받는 `SECURITY DEFINER` 다. 현재는 `tenant_status` CHECK 위반으로 실행 자체가 실패하므로(`601802` §5.2·§9.1) 사실상 도달 불가지만, **수리하면 모든 `authenticated` 사용자가 임의 tenant 를 격리하고(그리고 `p_isolate := false` 로 해제하고) `security_audit_log` 를 남길 수 있다.** `601810` `I-3`(축 변경은 별도 권한)·`I-32`(격리·해제 권한 분리)·`601801` `HG-A-2`(「플랫폼 특권 경로만 허용」)가 강제되지 않는다. `D-27`~`D-29` 는 GRANT 없이 닫히는데 `D-26` 만 열려 있고, 계약이 그 비대칭을 기술하지 않았다 | **YES** | 4 |
| **B-7** | 1 | `601812` §2.6 · §2.1 `D-3` · `601810` `I-49` | `D-3 tenant_billing_reviews` 테이블은 만들지만 **행을 생성하는 조작이 계약에 없다.** §2.6 의 `D-26` 본문 요건 5행에 review 생성이 없고, 오히려 **「subscription · 청구 관련 테이블을 쓰지 않는다」**가 들어 있어 `tenant_billing_reviews` 가 그 금지에 걸리는지 여부부터 해석이 갈린다. `601801` `HG-A-5`(「격리가 발생하면 billing review 대상 여부를 자동 판정한다 … review task 와 감사 기록을 생성한다」)와 `HG-A-9.4`(trial extension review)의 **자동 생성 요구가 네 문서를 거쳐 테이블 정의로만 남고 실행 주체가 사라졌다** | **YES** | 4 · 5 · 3 |
| **B-8** | 1 | `601812` §2.1 `D-2` · `601809` §2 `A-9` | `D-2 tenant_isolation_queue` 도 같다 — **쓰는 주체도 재처리하는 주체도 계약에 없다.** 외부 provider 수신 경로는 애플리케이션 코드이며 허용 파일이 아니다(§1·§7). `601810` `I-33`~`I-41`(`HG-A-7` 원문 보존 · 서명검증 · 폐기 금지 · quarantine · 재처리 idempotency)이 강제되지 않고, `601811` `TP-E-1`~`TP-E-6` 이 실행 불가다. `601809` §2 는 `A-9` 를 **구현 대상**으로 열거했는데 계약이 테이블만 남겼다 | **YES** | 1 · 4 |
| **B-9** | 1 | `601812` §2.2 `D-18`·`D-19` | `tenant_billing_reviews` 는 `tenant_id` 와 `isolation_event_id` 를 각각 **단일 컬럼 FK** 로만 건다. `tenant_isolation_events` 에 `(id, tenant_id)` 복합 UNIQUE 도, 복합 FK 도, tenant 일치 CHECK 도 없다. **tenant A 의 billing review 가 tenant B 의 격리 사건을 참조하는 행이 제약을 통과한다.** 이 워크패킷이 선행 나선의 사례로 지목한 「단일 컬럼 외래키가 존재만 검사하고 경계 일치는 검사하지 않는다」와 같은 형태다. `601811` 에 대응 Test 가 없다 | **YES** | 4 |
| **B-10** | 4 | `601812` §2.5 `D-27`·`D-28` | `D-28 is_tenant_access_allowed(p_tenant_id, p_operation)` 의 **`p_operation` 값 정의역과 `tenant_status` 5값 → 허용/거부 매핑이 계약에 없다.** 그런데 `601811` `TP-S-1`~`TP-S-10` 은 `TRIAL`+`NONE` 은 「`tenant_status` 축 허용」, `SUSPENDED`·`CANCELLED`·`TERMINATED`+`NONE` 은 「거부」라는 **구체 기대값**을 요구한다. `D-27` 도 마찬가지로 `TERMINATED` 분기(`601810` `I-24` · `TP-N-8`)와 `tenant_status` 불변(`I-25` · `TP-N-7`) 본문 요건이 기술되지 않았다(§2.6 은 `D-26` 만 규정). 구현자가 매핑을 스스로 만들어야 하고, `601801` `HG-A-10` 「2~4단계가 채우지 않는다」에 걸린다 | **YES** | 3 · 1 |
| **B-11** | 4 | `601811` `BL-6` · `601812` §2.6 | `BL-6` 의 after 기대는 **8**이며 근거를 「`D-28` 이 `tenant_status` 를 읽는다. 감소는 0이어야 한다」로 적었다. before 7 은 `601802` §6.2 의 7함수이고 그중 하나가 `isolate_tenant` 다. 그런데 §2.6 은 「`tenant_status` 를 **읽기만 한다**」로만 적어 **참조를 유지할 의무를 부과하지 않는다.** 수리의 자연스러운 형태(= `tenant_status` write 를 제거)는 참조를 통째로 없애고, 그러면 7−1+1 = **7** 이 되어 `BL-6` 과 `AC-1` 이 FAIL 한다 | **YES** | 1 · 3 |
| **B-12** | 4 | `601811` §2 · `PRE-3` · `601812` `V-3` | `PRE-3`·`V-3` 은 「`BL-1`~`BL-13` 재측정이 **`601802` 기록과 일치**한다」를 착수 게이트로 두고 불일치를 중단 사유로 삼는다. 그러나 `BL-7`(`catchmenu_hq` 함수 수) · `BL-8`(`tenants` 행 수) · `BL-9`·`BL-10`(두 축 값 분포) · `BL-11`(전 스키마 RLS policy 수) · `BL-13`(`merchant_accounts` 행 수 · 연결) **6건은 `601802` 에 기록이 없다.** `601802` 는 §7·§8 의 12테이블과 §6·§9 의 함수만 실측했다. `BL` 표 자신도 그 6건의 before 를 「착수 직전 실측」으로 적어 `PRE-3` 과 어긋난다. `AC-2`(`PRE-1`~`PRE-8` 전건 충족)를 만족시킬 수 없다 | **YES** | 1 |
| **B-13** | 2 | `601812` §7 · `601808` `N-7` · `601809` §2.1 `C-1` · `601803` `U-7` | `601808` `N-7` 은 **`OUT_OF_SCOPE`** 로 분류하며 「`U-7` 에 소관을 `0-C` 로 명시한다. **판정 위치를 `0-A-2` 안에서 확정하지 않는다**」로 지시했고, `601809` §2.1 `C-1` 이 착수 전 확인으로 그것을 승계했다. `601812` §7 은 「API 계층 · DB/RPC **둘 다에서 막는다**」, 「`601811` `TP-S` 는 **DB / RPC 계층에서 판정한다**」로 판정 위치를 확정하고 `D-28` 을 그 검사 함수로 신설했다. **상위 확정 문서 3건과 충돌한다** | **YES** | 6 |
| **B-14** | 2 | `601812` §2.1 `D-1` · §2.5 `D-29` · `601801` `HG-A-15` | `HG-A-15` 는 최종 귀책 확정에 **「분리 원칙 — 자기가 관여한 사건을 자기 혼자 승인하지 못한다」**를 선언했고 `HD-0-A-2-9` 가 영문으로 재확인했다(`601801` §3). `D-1` 은 `requested_by` 와 `confirmed_by` 를 모두 갖는데 **두 값이 달라야 한다는 제약도, `D-29` 본문 요건도 없다.** `601810` 이 이 선언을 `I-54`~`I-56` 으로 옮기면서 **분리 원칙 자체가 불변조건 목록에서 사라졌고** `601811` `TP-P-8` 은 「확정 이력 존재」만 본다. 또한 `D-8` 이 `provisional_attribution` 4값만 제약하고 **`confirmed_attribution` 에는 CHECK 도 값 정의역도 없다** — `D-29` 의 `p_attribution` 이 무엇을 받는지 미확정. `000221` §6.1 A급 항목이다 | **YES** | 5 · 4 · 3 |
| **B-15** | 3 | `601812` §2.1 `D-1` · §8.2 · `601811` `TP-P-7` | `D-1.provisional_attribution` 은 `NOT NULL` + `D-8` 4값 CHECK 인데, §8.2 시그니처 불변 판정으로 `isolate_tenant` 에 **분류 입력 인자가 없다**(`601802` §5.1 — `p_isolation_reason text` 뿐). **임시 분류값을 무엇으로 채우는지의 규칙이 계약에 없어** 구현자가 「항상 `UNKNOWN`」과 「사유 문자열 파싱」 중에서 스스로 고르게 된다. 전자면 `601810` `I-54` 와 `TP-P-7`(「임시 분류값이 기록되고 임시임이 표시된다」)이 형식만 통과하고 `HG-A-15` 의 1차 분류 요구가 실질을 잃는다 | **YES** | 3 · 5 |

---

## Findings — informational

| # | 축 | 지점 | 내용 |
|---|---|---|---|
| N-1 | 2 | `601812` §2.1 · `601702` §1.45 | 세 신규 테이블을 `catchmenu_common` 에 두면서 **배치 근거를 적지 않았다.** `601702` §1.45 는 「특별한 분리 근거가 없는 한 schema 를 가르지 않는다. **가르면 새로운 schema boundary 를 설명해야 한다**」고 요구한다. `catchmenu_hq.tenants` 를 FK 로 참조하므로 설명이 필요한 경계다. 다만 `isolate_tenant` 와 기존 `security_audit_log`·`idempotency_keys` 가 `catchmenu_common` 에 있어(`601802` §5.1·§8.1) 배치 자체는 정합적이다 |
| N-2 | 4 | `601811` `BL-14` | 근거란이 신규 함수 3건을 「`D-26`·`D-27`·`D-29`」로 적었다. `D-26` 은 `CREATE OR REPLACE`(기존 함수)이고 신규는 **`D-27`·`D-28`·`D-29`** 다. 기대값 `before + 3` 자체는 맞다 |
| N-3 | 4 | `601811` `AC-1` · `PRE-3` · `601812` `V-3` | 세 곳 모두 `BL-1`~**`BL-13`** 만 범위로 삼아 **`BL-14` 가 어느 acceptance 에도 걸리지 않는다.** `TP-R-1` 만 간접 참조한다 |
| N-4 | 4 | `601811` `TP-N-4` · `601812` §4.1 `X-4` | 「`detect_threat` 및 나머지 호출 금지 **5**함수」로 적었으나, 7건에서 `isolate_tenant`·`manage_subscription`·`detect_threat` 를 빼면 **4**건이다(`BL-4` 는 「4함수」로 정확히 적었다). `TP-R-5` 의 6함수와도 세는 방식이 다르다 |
| N-5 | 4 | `601812` §5 · §4.2 `FO-13` | 「같은 키 · **같은 내용** 기존 결과를 반환 / 다른 내용 거부」의 **내용 비교 대상 컬럼이 지정되지 않았다**(`D-1` 에 `request_hash` 계열 컬럼 없음). `FO-13` 이 `EXCEPTION` 절을 금지하므로 unique 위반 포착형 구현이 막히고 `ON CONFLICT` + 선행 SELECT 형태로 좁혀진다. 구현은 가능하나 형태 선택이 남는다 |
| N-6 | 1 | `601802` §8.1 · `601803` `U-8` · `601812` | `601803` `U-8` 은 **`offline_queue` 와 `idempotency_keys` 두 기존 자산**의 재사용 가부를 4단계로 이월했다(`601808` `N-8`). `601812` 는 `offline_queue` 를 언급하지 않고 `idempotency_keys`(21컬럼 · `UNIQUE(tenant_id, key_domain, idempotency_key)` · `provider_event_id` · `replay_count`)와 `D-1`·`D-2` 의 관계도 판정하지 않은 채 병행 구조를 신설한다. `HG-A-7` 이 요구한 「구조 · 권한 · 보존기간을 조사한 뒤 결정」이 `offline_queue` 에 대해서만 형식적으로 닫혔다 |
| N-7 | 1 | `601812` §2.1 `D-1` · `601811` `TP-N-17` | `601810` `I-20`(자발적 사용 중지를 `ISOLATED` 로 표현하지 않는다 — `601801` §2)에 대응하는 물리 강제가 없다. `isolation_reason` 은 CHECK 없는 자유 text 다. `TP-N-17` 「기록하는 경로가 0」의 판정 기준이 계약에 없다. 다만 `601801` §2 의 5분류를 닫힌 enum 으로 선언한 적은 없으므로 제약 미신설 자체는 `HG-A-10` 과 모순되지 않는다 |
| N-8 | 3 | `601811` §12.1 | `I-49`~`I-53` 의 SKIP 사유가 「billing review task 의 물리 표현이 `601812` 에서 정해진다」인데 `D-3` 이 그것을 확정했다. **사유가 소멸했는데 대응 Test 가 신설되지 않았다.** `AC-5`(각 `I-N` 이 검증 또는 미검증 사유를 갖는다)가 형식으로만 충족된다 |
| N-9 | 4 | `601812` §2.1 `D-2` · §2.2 `D-12` | `replay_key` 는 `NOT NULL` 이지만 UNIQUE 가 없다. `601810` `I-39`(해제 후 재처리의 중복 적용 방지)의 물리 근거는 `D-12 UNIQUE(tenant_id, provider_code, provider_event_id)` 뿐이며, 그것은 **수신 중복**을 막을 뿐 **재처리 중복**을 막지 않는다 |
| N-10 | 4 | `601812` §0.1 · §2.4 `D-25` | §0.1 이 `D-N` 을 「허용 DDL」로 정의했는데 `D-25` 는 **「만들지 않는다」**를 `D-N` 번호로 등재했다. `§2.7` 대응표에도 「`D-25`(비생성)」로 남는다 |
| N-11 | 4 | `601811` `TP-R-1`·`TP-R-2` | 함수 회귀는 `catchmenu_hq` 와 `catchmenu_common` 두 스키마만 센다. `601802` §9.1 은 `record_van_transaction`(`catchmenu_payment`) · `check_staff_permission`(`catchmenu_store`)를 실측했으므로 **그 스키마들의 함수 증감은 검사되지 않는다**(개별 md5 는 `TP-R-5` 가 본다) |

---

## 수직 추적표

`Human 선언 → 불변조건 → Test → 허용 조작` 15건 전부.

| `HG-A-N` | Logic (`601810`) | TestPlan (`601811`) | ChangeContract (`601812`) | 완결 |
|---|---|---|---|---|
| `HG-A-1` 상태축 독립성 | `I-1`·`I-4`·`I-13` | `TP-S-12`·`TP-S-1`~`TP-S-10`·`BL-12` | `D-28` · §9 `FO-15` | **△** — 정의는 완결. `D-28` 을 호출하는 경로 없음(**B-1**) |
| `HG-A-2` `ACTIVE`+`ISOLATED` 의 의미 | `I-12`·`I-19`·`I-40` | `TP-P-9`·`TP-E-6`. `I-19` 는 §12.1 미검증(`0-C`) | `D-28` · §7 | **✕** — 차단 9행은 `U-16` 이월, 강제 주체 부재(**B-1**) |
| `HG-A-3` 격리 ↔ 상업 상태 분리 | `I-26` | `TP-N-1`·`TP-N-2`·`TP-N-3` | §2.6 `D-26` 본문 요건 · `FO-5` | **○** |
| `HG-A-4` 기본료 · 사용량 과금 | `I-42`~`I-45` | `TP-E-8`·`TP-E-9`. `I-42`·`I-43` 은 §12.1 미검증 | `D-2.usage_counted` · `X-6` | **△** — 큐 생산자 부재로 `TP-E-8`·`TP-E-9` 실행 불가(**B-8**) |
| `HG-A-5` Human 과금 검토 | `I-48`~`I-51` | `TP-N-12`·`TP-N-13`. `I-49`~`I-51` SKIP | `D-3`·`D-17`~`D-22` | **✕** — review 자동 생성 주체 부재 + §2.6 과 충돌(**B-7** · N-8) |
| `HG-A-6` 구독 변경 ↔ 해제 분리 | 불변조건 없음(§0 · §3 ⚠️) | `AC-8`·`TP-N-3` | `FO-2` · `X-1` | **○** — 이월이 이월로 명시됨 |
| `HG-A-7` 외부 이벤트 · side effect 격리 | `I-33`~`I-41` | `TP-E-1`~`TP-E-6` | `D-2`·`D-12`~`D-16` | **✕** — 수신 · 재처리 주체 부재(**B-8** · N-9) |
| `HG-A-8` 격리 해제 승인 3요건 | `I-22`·`I-23` | `TP-P-4`·`TP-P-5` | `D-27` · `D-1.cause_resolved`·`human_approved` | **✕** — 요건 성립 조작 없음(**B-3**), `D-26` 우회 경로(**B-2**) |
| `HG-A-9` 10조합 합성규칙 (`9.1`~`9.8`) | `9.1`→`I-5` · `9.2`→`I-13`·`I-14` · `9.3`→`I-46` · `9.4`→`I-52`·`I-53` · `9.5`·`9.6`→`I-25` · `9.7`→`I-24` · `9.8`→`I-2`·`I-3` | `TP-S-11`·`TP-S-1`~`10`·`TP-N-7`·`TP-N-8`·`TP-N-11`·`TP-R-9`. `9.4` 는 SKIP | §9 `FO-15`(제약 미신설) · `D-28` · `D-27` · `FO-5` | **△** — `9.1`·`9.8` 완결. `9.2` 는 **B-1**, `9.7` 은 `D-27` 분기 미기술(**B-10**), `9.4` 는 **B-7** |
| `HG-A-10` 초기값 · 전이 · 멱등성 · 동시성 | `I-21`·`I-27`~`I-32`. `I-27`·`I-28`·`I-32` 는 §12.1 미검증 | `TP-C-1`~`TP-C-5`·`BL-9` | §5(`Q-2`)·§6(`Q-3`)·`D-6` | **✕** — `idempotency_key` 입력 부재로 `Q-2` 판정이 성립하지 않음(**B-4**) |
| `HG-A-11` 이 나선이 소유하는 2축 | `I-6`·`I-7`·`I-8`·`I-9`. `I-8` 은 §12.1 미검증 | `TP-N-10`·`TP-N-1`·`BL-6`·`BL-10` | `FO-5`·`FO-8` · `X-5` | **△** — 금지 조작은 완결. `BL-6` 기대값 불성립 위험(**B-11**) |
| `HG-A-12` `MerchantAccount` 경계 | `I-10`·`I-11` | `TP-N-9`·`TP-R-8`·`BL-13` | `FO-9` · `X-2` | **○** |
| `HG-A-13` 외부 제품 경계와 격리 범위 | `I-15`~`I-18` | `TP-N-16`·`TP-P-10` | `D-28` · §7 | **✕** — `TP-P-10` 의 API 계층이 허용 파일 밖(§1·§7)이라 실행 불가(**B-1**) |
| `HG-A-14` tenant 별 포인트 권위 | 불변조건 없음 | `AC-9` | `FO-16` · `X-7` · §0.2 ⑤ | **○** — 이월이 이월로 명시됨 |
| `HG-A-15` 격리 원인 귀책 확정 주체 | `I-54`~`I-56` | `TP-P-7`·`TP-P-8`·`TP-N-12` | `D-1`·`D-8`·`D-29` | **✕** — **분리 원칙이 `601810` 에서 소실**(**B-14**), 임시 분류 입력 부재(**B-15**) |

**완결 ○ 5 · △ 4 · ✕ 6.**

---

## 확인 불가 항목

첨부 9건 밖의 자료가 필요해 판단하지 못한 것이다. 추정으로 채우지 않았다.

| # | 항목 | 왜 확인 불가인가 |
|---|---|---|
| 1 | `isolate_tenant` 본문 전문 — 내부 권한 검사 · actor 검증의 존재 여부 | `601802` §5.1·§5.2 는 md5 · 길이 · 참조 대상만 기록한다. 본문이 첨부에 없다. **B-6** 의 실제 노출 정도는 이 확인에 종속된다 |
| 2 | 세 신규 테이블과 네 함수의 **소유 role** · `catchmenu_authority_owner` 의 `BYPASSRLS` 여부 | `601802` §7.3·§8.2 는 GRANT grantee 만 기록하고 owner 를 기록하지 않는다. **B-5** 의 「FORCE RLS 하에서 INSERT 실패」가 실제로 발생하는지 판정 불가 |
| 3 | `catchmenu_hq.tenants` 의 RLS enable/force · policy | `601802` 는 `catchmenu_common` 12테이블만 측정했다. `D-26` 의 `tenants` UPDATE 가 어떤 조건에서 성립하는지 판정 불가 |
| 4 | `BL-7`·`BL-8`·`BL-9`·`BL-10`·`BL-11`·`BL-13` 의 before 실측값 | 어느 첨부에도 없다. **B-12** 는 「기록이 없다」는 사실만 근거로 삼았다 |
| 5 | `sql/migrations/0170`·`0171`·`0172` 의 내용 및 `0172` 파일명 충돌 여부 | migration 원문이 첨부 아님 |
| 6 | `601505` §4 호출 금지 조항 원문 · `600010` §1.1 원문 | `601809` §4.4 의 7건 목록과 해제 조건이 원문과 일치하는지 대조 불가 |
| 7 | `601717` `N-4′` · §6 · §10.9 `V-24`, `601746` §2.7, `601744` `F-7`, `601716` §0.2, `601713` §3, `601747` | `601811`·`601812` 가 형식 · 선례로 인용했으나 원문이 첨부 아님. 승계의 정확성 확인 불가 |
| 8 | `601804`~`601807` 원문 | `601809` §5.2 · `601810` §9.2 · `601811` §13.2 가 「`601808` 이 전건 승계했다」로 배제했다. 승계 누락 여부를 독립 확인할 수 없다 |
| 9 | `000221` §3.2 · §4.4 · §6.1, `000220` §4, `000701` §14.5 · §37 · §46 · §47.1 | 인용된 규범 원문이 첨부 아님. 인용의 정확성은 대조하지 않고 문서 간 정합만 봤다 |
| 10 | `D-26` 수리 후 `security_audit_log` · `catchmenu_ledger.events` 기록의 유지 여부 | `601802` §5.2 는 현재 두 곳에 INSERT 한다고 기록하나, `601812` §2.6 은 `tenant_isolation_events` 만 요구하고 나머지를 언급하지 않는다. **계약 자체의 공백**이므로 `B-10` 계열이나, 기존 감사 소비자 존재 여부는 확인 불가 |

---

**발견 0건이 아니다.** blocking 15 · informational 11.

**처분은 하지 않았다.** 통합자가 정한다.

## §2 Status

```text
Audit evidence only.
```

**통합은 `601816` 이 수행한다. 처분은 Human 이 정한다.**
