# 600455_Module.md

Status: Implemented
Lifecycle: Module
Stage: 4
Owner: Codex
Date: 2026-07-13

## Summary

Implemented the approved `place_takeout_order_unassigned_record_fix` change (`600454_ChangeContract.md`): replaced the untyped `record` variables `v_customer`/`v_coupon` in `catchmenu_store.place_takeout_order()` with scalar variables, covering all 11 confirmed `.id`/field reference points, plus the point policy A안 (Human decision) branch.

| File | Purpose | Result |
|---|---|---|
| `sql/migrations/0081_create_customer_app_rpc.sql` | `place_takeout_order()` only — `v_customer`/`v_coupon` `record` declarations replaced with `v_customer_id`/`v_customer_display_name`/`v_customer_membership_tier`/`v_customer_point_balance` and `v_coupon_id`/`v_coupon_discount_type`/`v_coupon_discount_value`/`v_coupon_discount_pct`/`v_coupon_min_order_amount`/`v_coupon_max_discount_amount`; all 11 `.id`/field reference sites (L609/L622/L653/L661/L714/L852/L864/L869/L924/L930/L949) converted; `elsif v_customer_id is null and p_use_points > 0` branch added calling `catchmenu_common.log_diagnostic()` with `p_log_level := 'WARNING'`. | Applied, live-verified. |

## Boundary Notes

- Only `place_takeout_order()` was touched — `track_takeout_order()` and every other function in `0081` confirmed unchanged (diff scoped exactly to the approved function boundary, `600454_ChangeContract.md` §1/§2).
- `discount_pct`/`'AMOUNT'`/`'PCT'` literals were **not** touched, as forbidden by `600454_ChangeContract.md` §2/§6.1 — the discount-calculation `CASE` still reads `v_coupon_discount_type`/`v_coupon_discount_value`/`v_coupon_discount_pct`, unchanged in substance from the pre-fix code, only renamed from record-field to scalar-variable access.
- `600440`'s `READY_TO_COMMIT`→`COMMITTED` changes elsewhere in the same file (`track_takeout_order()`) confirmed preserved (0 occurrences of `READY_TO_COMMIT` remain in the file).
- Checksum bookkeeping was correctly updated this time — no `0070`/`0081`-style drift (unlike the prior `600440` workpacket's gap): current file checksum and `catchmenu_meta.migration_history` match exactly.
- No cloud database was touched. No git commit was performed.
