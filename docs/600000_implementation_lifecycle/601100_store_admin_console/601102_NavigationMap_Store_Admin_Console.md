# 601102_NavigationMap_Store_Admin_Console.md

Status: Active
Lifecycle: NavigationMap
Domain: Store Admin Console

## Workpacket Flow

| Workpacket | Scope | Local flow |
|---|---|---|
| `601110_store_admin_sql_layer_reconciliation/` (Change ID: `store_admin_menu_rpc_correction`) | Menu management RPC phantom-column repair — `catchmenu_store.upsert_menu()`/`set_menu_status()`/`get_menu_admin_list()` (`0110`) reference 4 phantom columns (`thumbnail_url`→`image_url`, `allergen_codes`→`allergen_info`, `menu_options`→relational `menu_option_groups`/`menu_option_items`, `pos_sync_at`→no live column), plus the shared `allergen_codes` crash in `get_store_admin_dashboard()`. Redesigned as a 3-tier structure (Option C, Human decision): public `upsert_menu()` (unchanged signature) delegating to internal `upsert_menu_core()` + `sync_menu_option_groups_core()` + `sync_menu_option_items_core()`, full-replacement contract for options (soft-delete via `is_active=false`). Discovered mid-design that `allergen_info` is a JSONB object (not array, per `chk_menu_allergen_object`) and that `price_delta` (not `additional_price`) is the actual canonical option price field, contradicting the Overview's initial guess. The Price List architecture extension (originally drafted here as §7-§12) was split out to `601130` once its scope/risk proved fundamentally different — see that row. **`601114` §8 APPROVED (items 1-7, 2026-07-17), §11 Final Audit Verdict: ACCEPT (2026-07-17).** Implemented as an in-place synchronization of `sql/migrations/0110_create_store_admin_rpc.sql` (no new migration number — same convention as `601140`), committed (`3e84a83e`). | `601111_Overview_Store_Admin_Sql_Layer_Reconciliation.md` → `601112_Logic_Store_Admin_Menu_Rpc_Correction.md` → `601113_TestPlan_Store_Admin_Menu_Rpc_Correction.md` → `601114_ChangeContract_Store_Admin_Menu_Rpc_Correction.md` (APPROVED, ACCEPT) → `0110_create_store_admin_rpc.sql` (in-place sync, implemented, live) — no separate Module/Verification/Audit files; Stage 8-11 evidence is recorded inline in `601114` §8-§11 |
| `601120_dining_table_crud_creation/` | Dining table admin CRUD (create/list/deactivate `catchmenu_store.dining_tables` records) — split out from `601110` 2026-07-16 (Human decision, rollback/verification/traceability separation). Existing operational RPCs (`update_table_status()`/`get_table_floor_map()`/`register_table_qr()`/`release_table()`, `0048`) confirmed phantom-free; only CRUD is missing (currently seeded via direct `INSERT` in `0034` only). Investigation preserved as a starting point in `601111_Overview_Store_Admin_Sql_Layer_Reconciliation.md` §4 (dining_tables has 20 live columns, not the originally-assumed 12; minimal create-time input is `table_code`+`capacity`, with QR/NFC/device-binding deferred to existing separate-step RPCs). **`601124` §10 APPROVED (2026-07-17), §11 Final Audit Verdict: ACCEPT (2026-07-17).** Implemented as `sql/migrations/0162_create_dining_table_admin_rpc.sql` (`upsert_dining_table()`/`set_dining_table_active()`/`get_dining_table_admin_list()`), committed (`da98430c`); the Final Audit addition to `601124` committed separately (`0d76ecc9`). | `601121_Overview_Dining_Table_Crud_Creation.md` → `601122_Logic_Dining_Table_Crud_Creation.md` → `601123_TestPlan_Dining_Table_Crud_Creation.md` → `601124_ChangeContract_Dining_Table_Crud_Creation.md` (APPROVED, ACCEPT) → `0162_create_dining_table_admin_rpc.sql` (implemented, live) — no separate Module/Verification/Audit files; Stage 8-11 evidence recorded inline in `601124` |
| `601130_menu_price_list_architecture/` | Multi-channel menu price list architecture — replaces the "one menu = one price" model (`menus.price` single column) with a price-list-based model (`price_lists`/`price_list_assignments`/`menu_prices`/`option_item_prices`) and a single canonical resolver `resolve_menu_price()` (priority: store+provider > store+channel > store default > brand+channel > brand default > menu base price). Split out from `601110`'s Logic doc 2026-07-16 (Human decision, §12 (g) confirmed) once its scope (4 new tables + canonical resolver + migration across 9 confirmed live consumers of `menus.price`, including `create_order()`) proved fundamentally different in scale/risk from the phantom-column repair. Investigation confirmed: no 900xxx design document describes multi-channel pricing at all (a genuinely new direction, not an unfinished prior design); `order_items` already has two generations of price-snapshot columns, with the newer (`0141`-added) generation completely unwired by any live function — the same phantom-column pattern found again one layer down; live data is trivial (1 store, 9 menus) so migration risk is entirely about consumer cutover order, not data volume. Migration path designed as fully additive/non-destructive (`menus.price` becomes the permanent fallback tier, never removed). **Re-confirmed accurate (2026-07-18)** — still only 2 documents exist, `601132_Logic.md` §5 still carries several unresolved Human-decision Open Items, and this whole folder is **not yet committed to git** (untracked). | `601131_Overview_Menu_Price_List_Architecture.md` → `601132_Logic_Menu_Price_List_Architecture.md` (Stage 1.5 complete — investigation + schema/resolver/migration/integration design) → (TestPlan/ChangeContract/Module/Verification/Audit pending, several Human-decision Open Items remain — see `601132_Logic.md` §5) |
| `601140_allergen_info_and_sibling_overwrite_correction/` | Parameter-default sibling-overwrite bug in `upsert_menu_core()`/`upsert_menu()` — a different defect class from `601110`'s phantom-column naming issue (this one is about non-null hardcoded parameter defaults silently resetting `allergen_info`/`is_kds_required`/`kitchen_zone`/`display_order` to a hardcoded default on any partial update that omits them), discovered during `601110`'s Stage 9 independent re-verification and deliberately kept as a separate workpacket rather than reopening `601110`'s already-closed approval boundary. Also fixed a related Stage-8-discovered regression risk in category `display_order` handling (an emergency `coalesce(p_display_order, 0)` fix would have silently reset existing categories to `0` — caught by ChatGPT+Gemini cross-verification before it shipped; fixed by splitting the INSERT/UPDATE paths instead). **`601144` §10 APPROVED (2026-07-17), §11 Final Audit Verdict: ACCEPT (2026-07-17).** Implemented as a second in-place synchronization of `sql/migrations/0110_create_store_admin_rpc.sql` (same convention as `601110`, no new migration number), committed (`88d3651c`). **`601144`'s own §11 Final Audit section is currently a local uncommitted addition** (`git status` shows it modified) — the SQL implementation itself is fully committed and live; only this specific documentation addition is not yet in git history. | `601141_Overview_Allergen_Info_And_Sibling_Overwrite_Correction.md` → `601142_Logic_Allergen_Info_And_Sibling_Overwrite_Correction.md` → `601143_TestPlan_Allergen_Info_And_Sibling_Overwrite_Correction.md` → `601144_ChangeContract_Allergen_Info_And_Sibling_Overwrite_Correction.md` (APPROVED, ACCEPT — audit section uncommitted) → `0110_create_store_admin_rpc.sql` (in-place sync, implemented, live) — no separate Module/Verification/Audit files |

See `601100_Readme_Store_Admin_Console.md` for the domain-level narrative (Purpose/In Scope/Out of Scope).
